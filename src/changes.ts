/**
 * Что нового: список изменений, которые видно человеку.
 *
 * Пишется руками, в том же коммите, что и само изменение, — правило
 * в `CLAUDE.md`. Сборка без видимых изменений строки не получает.
 *
 * Числа в строках — история: правда в день записи и остаётся ею. Правило
 * «текст из констант» — для текстов, которые описывают, как приложение
 * работает сейчас.
 *
 * Механика — что показать после обновления, какой id прочитан — ядра
 * (`shared/screens/changes.ts`, Я-06); здесь только список.
 */

import type { Change } from './shared/screens/changes.ts'

/** От старых к новым. Новая запись — в конец. */
export const CHANGES: readonly Change[] = [
  {
    id: 1,
    date: '2026-09-24',
    lines: [
      'Первая сборка: ставится иконкой, открывается без сети, обновляется сама. Сводки ещё нет — будет следующим шагом.',
      'Своё название приложения — «Настройки» → «Название».',
      'Копия данных — «Настройки» → «Копия данных»: сохранить в файл и восстановить.',
    ],
  },
  {
    id: 2,
    date: '2026-09-25',
    lines: [
      '«Семья»: приложения добавляются, правятся и убираются — имя, репозиторий данных, сайт, порядок.',
      '«Настройки» → «Токен чтения»: вписать, заменить, забыть и проверить доступ к репозиторию каждого приложения.',
    ],
  },
]
