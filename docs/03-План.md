# План

Обновлён: 25.09.2026 (Р-15)

Здесь только то, что предстоит. Что уже сделано — в Журнале.

## Логика порядка

Сначала каркас с установкой и автообновлением на телефоне: этот риск ломается тихо и вскрывается через неделю. Потом — «Сводка» на настоящем срезе «Делу Время»: ради неё метаприложение и затеяно, и ею закрывается Этап 3 ядра. «Семья» — следом: она мала, но требует проверки на телефоне. Сопоставление групп — когда срез отдадут «Дневники»: у «Трапезы» групп в срезе нет, настоящей пары раньше нет (Р-15). Бот — последним: он новый потребитель и требует правки состава договора.

---

## Хвосты закрытых этапов

- **Этап 0:** заглушка синхронизации (Р-09) уходит, когда ядро сделает `sync` в `Core` необязательным — обновлением указателя, сессия ядра
- **Этап 1:** не хвост, а сессия ядра — закрыть её Этап 3 и пункты из Журнала 25.09.2026, «Для сессии ядра» (`canWrite`, подключение «в пару кликов»). Здешней работы не держит
- **Этап 2:** ссылки на iPhone не проверены — когда будет iPhone. Риск — Safari вместо установленного соседа, соседа без данных (Р-14, «Цена»)

## Этап 3 — Рядом: сопоставление групп соседей

Вопрос этапа: как увидеть рядом строки разных приложений про одно и то же, не складывая их (Я-19, Я-15)?

Начинается, когда срез отдают «Дневники» и он виден на «Сводке» (Р-15): у «Трапезы» срез есть, но групп в нём нет — пары для «рядом» почти нет. До того, по порядку: сессия ядра, «Дневники», «Злотые» — промпты в Журнале (25.09.2026, «Промпты» и запись о Р-15). Срез «Злотых» с не календарным месяцем — сначала правило сопоставления месяца (`05`, открытые вопросы). Настройка человека «эти строки — рядом», по `key`; переименование группы «Делу Время» меняет ключ — настройку поправляет человек (Я-17, «Цена», п. 3). Решение — в начале этапа.

## Этап 4 — Телеграм-бот

Вопрос этапа: приходит ли сводка недели в Телеграм в выбранное время без открытого метаприложения?

Черновик, решается в начале этапа:

- **Исполнитель** — плановый workflow GitHub Actions: читает срезы токеном из секретов, `parseSummary` ядра, текст — теми же функциями `view/`, отправка — Bot API. Сервер не нужен
- **Где живёт.** Логи Actions публичного репозитория видны всем (Р-01): либо **отдельный приватный репозиторий** (бесплатных минут хватит; `TotalUchet` — подмодулем ради `view/`), либо здесь, но в лог — ничего из среза. Рекомендация — приватный
- **Расписание Actions неточно** — опоздание на минуты, бывает до часа («Делу Время» отвергло Actions для напоминаний по этой причине). Для недельной сводки терпимо. В публичном репозитории плановые workflow выключаются после 60 дней без коммитов — ещё один довод за приватный
- **Секреты:** второй экземпляр токена чтения, токен бота, id чата
- **Бот — новый названный потребитель** (Я-13, Я-14), **Телеграм — третья сторона:** что ему уходит — правка состава договора сессией ядра, **до кода**
- **Бот с командами** — позже: нужен сервер или вебхук

## Отложено

- **Уведомление вместо четырёх** — после решения ядра по Я-18: как приложения перестают напоминать о темах из среза
- **Экран динамики по архиву** — когда архив накопит месяцы; до экрана — пересмотр Я-15 сессией ядра (Р-08)
- **Синхронизация архива и списка приложений** — второй токен; когда понадобится второе устройство с архивом (Р-02, Р-08)
- **Общая хроника с поиском** по `FeedItem` приложений — в договоре срезов её нет; отдельный разговор с ядром
- **Аналитика связей и ИИ** — когда будут данные (Я-10)
- **Подключение «в пару кликов»** (человек, 25.09.2026). Сейчас другу или новому устройству надо вписать токен в каждое приложение и имена репозиториев в «Семье» руками — по восемь раз. Своё второе устройство — то же: список приложений «Тотального Учёта» и архив едут только файлом-копией, токен — руками (Р-02, Р-08). Нужно: настроить семью на устройстве за пару действий и переносить между своими устройствами автоматически. Вопрос общий для семьи — сессия ядра; рядом — открытый вопрос ядра «токен один раз на устройство для всех приложений» (Я-25)
- **Быстрое продление токенов или синхронизация без ручных токенов** (человек, 25.09.2026). Истёк токен — перевписывать во все приложения на всех устройствах (Я-27, «Цена»). Либо простой способ обновить разом, либо другой способ синхронизации, не требующий токена руками. Сессия ядра
