# Журнал

Что сделано и в каком оно состоянии. Отвечает на вопрос «где мы сейчас».
Обновляется в конце каждой беседы. Новые записи сверху.

---

## 25.09.2026 — Этап 3: связки — код сделан (Р-17…Р-22); проверка на телефоне — за человеком

Та же беседа, после ответа ядра. Развилки — по одной, с рекомендацией; все шесть — выбор человека, записаны в момент выбора.

**Решения**

- **Р-17** — «рядом» — связки: имя человека и строки (приложение + `key`), выбранные из увиденных срезов; строка — не больше чем в одной связке; своего числа у связки нет
- **Р-18** — хранилище `bundles` с `Base`, схема 2, первая миграция `additive: true`, место `bundles.json`; едут файлом-копией вместе с `apps`
- **Р-19** — связка на «Сводке» между переключателем и приложениями, `Fold`; по приложениям со своей свежестью, столбиком; у свёрнутой — имена приложений; блоки приложений не меняются
- **Р-20** — строки нет: «нет в срезе за этот отрезок» или «нет ни в одном отрезке — поправь связку»; без догадок, ключ сам не переназначается; подпись — из архива; строки убранного приложения остаются в записи
- **Р-21** — связки правятся в «Семье», «Сводка» ведёт туда ссылкой `?bundle=<id>`
- **Р-22** — строки связки любые: ни запрета, ни пометки по смыслу ключа (`kcal.perDay` «Трапезы», `norm.*` у двоих)

**Сделано** — три коммита, запушены вместе:

- `0d4e2d9` — схема 2: `Bundle`, `V1_STORES` отдельно от `STORES`, миграция на 2, место, `storeNotes`, «О приложении» — «Связки». Тесты: база версии 1 с записями доезжает до 2 (`createLegacyBase` ядра), свежая база проходит тот же шаг, файл-копия версии 1 принимается, связки в `exportAll`
- `fe097e8` — без React: `app/bundles.ts` (занятые строки, проверка формы; `ordered`, `nextOrder`, `parseOrder` в `apps.ts` — общие), `view/rows.ts` (строки среза, подписи по архиву), `view/bundles.ts` (связка по приложениям, пропавшие строки, итог у свёрнутой, строки формы), `reading/seen.ts` — `archiveLabels`. Ни одной функции, которая берёт две строки и отдаёт число (Я-15)
- экраны: `screens/Bundles.tsx` — блок на «Сводке» и раздел «Связки» в «Семье» с формой; `ui/useBundles.ts`; прогон; `02`; «Что нового» — запись 6

*Проверено машинно:* `npm test` — 11 файлов, 87 тестов; `npm run typecheck`; `npm run build`; `npm run smoke` — 70 проверок; все с кодом 0. В прогоне — выдуманный «Пруд» вторым срезом: связка заведена в «Семье», строка из неё в новой форме недоступна; на «Сводке» — после переключателя и до приложений, два «посчитано», строки с основанием, «Улов — нет в срезе за этот отрезок» на неделе и число на месяце; у свёрнутой — «Полка · Пруд»; срез без «Улова» → «нет ни в одном отрезке — поправь связку» → ссылка открывает форму этой связки в «Семье», строка помечена; убранное приложение — отдельной группой в форме и не видно на «Сводке». Прогон падал на своей проверке — регистр заголовка (`text-transform`), поправлена проверка, а не код.

*Не проверено:* телефон — План, Этап 3, «Готово когда»; настоящие срезы в связке; вёрстка формы на узком экране глазами.

**Находки по ходу**

- **Подпись пропавшей строки может потеряться.** Архив хранит один срез на приложение и день расчёта (Р-08): срез, пересчитанный в тот же день уже без строки, перезаписывает запись, где она была. Подпись берётся из более раннего дня, а если строка жила один день — показывается ключ (в прогоне так и вышло: `pond.fish`). Для группы «Делу Время» ключ — нормализованное название группы: человек его узнает
- **Подпись в связке не хранится** (Р-17) — поэтому и нужен архив. Хранить подпись на момент выбора — второе место правды о строке; не делал

**Не описано в документах — на обсуждение**

- Порядок строк внутри приложения в связке — порядок хозяина; пропавшие — после, в порядке выбора. Решено кодом, не записью
- Связка из одного приложения не запрещена — это просто подборка его строк

**Следующий шаг** — человек: телефон по Плану, Этап 3, «Готово когда»; итог — сюда. Тогда Этап 3 закрыт; дальше — Этап 4 (бот), он начинается с сессии ядра (состав договора).

---

## 25.09.2026 — Ответ ядра: указатель `cb72d16`, заглушки синхронизации нет (Р-16)

Шаг 1 из «Передачи», в той же беседе: `/clear` не делали — человек, «рано».

**Решение** — **Р-16**: `CoreProvider` без `sync` (Я-29), отменяет Р-09. Рекомендация ядра, человек согласился.

**Сделано**

- Указатель `src/shared` `ee1dc37` → `cb72d16`
- `app.tsx` — `<CoreProvider value={{ config, db }}>`; `app/core.ts` — только `db`, `noSync` убран
- Я-28 — правки нет: комментарий `canWrite` в `reading/read.ts` уже говорит «права аккаунта, у владельца всегда `true`»
- Прогон: отчёт об ошибке открылся, и в нём «Синхронизация: выключена» — слова ядра из `ui/report.ts`, видны в окне отчёта
- `02` — `core.ts` без `noSync`; План — хвост Этапа 0 и «до развилок» сняты. Строки в `changes.ts` нет: видимого не поменялось

*Проверено машинно:* `npm test` — 9 файлов, 64 теста; `npm run typecheck`; `npm run build`; `npm run smoke` — 55 проверок; все с кодом 0.

*Не проверено:* телефон — «Сводка», «Семья», отчёт «выключена» (План, «Хвосты»).

**Следующий шаг** — развилки Этапа 3, первая: как человек настраивает «рядом».

---

## 25.09.2026 — Срезы отдают все четыре; ядро `cb72d16` (Я-28, Я-29); Этап 3 — следующим

Человек провёл три сессии по записи ниже: ядро, «Дневники», «Злотые». **«Дневники» и «Злотые» видны на «Сводке»** — со слов человека, «всё отображается». Условие Р-15 выполнено. Кода здесь нет — сверка через `git show` и запись контекста.

**Ядро — `cb72d16`** (здесь подмодуль ещё на `ee1dc37`). Журнал `FamilyCore`, 25.09.2026, «Этап 3 закрыт; „Трапеза“ в „Составе“; Я-28…, Я-29…»:

- **Этап 3 ядра закрыт** «Сводкой» на настоящем срезе. Строка «Трапезы» в «Составе» переписана: отдаёт, Р-55 «Трапезы», `assumed` убран. В `02` ядра — абзацы «Устойчивость ключей» и «Даты и „да/нет“ в `Value` нет» (описание, не правка договора)
- **Я-28** — «Проверить доступ» не обещает запись; `canWrite` — права аккаунта. Нас не касается: `SyncSettings` мы не ставим, Р-12 в силе
- **Я-29** — `sync` в `CoreProvider` необязателен; `useSyncStatus` без него — `off`. Наша заглушка `noSync` (Р-09) уходит обновлением указателя
- **Подключение «в пару кликов»** — открытыми вопросами `05` ядра с рекомендациями; решать — отдельной сессией ядра. Каталог семьи (`dbName` → имя, сайт) одинаков у всех — может жить в нашем коде, личное — только имена репозиториев: наше решение, когда дойдём
- **Промпт нам** — там же, «Промпт „Тотальному Учёту“»: указатель, `noSync`, комментарий `canWrite` в `reading/read.ts` сверить со словами Я-28, документы. Первым шагом следующей сессии, до развилок Этапа 3

**Срезы — что отдают** (таблицы целиком — `02` каждого приложения, «Срез итогов»):

| Приложение | Решение, код | Отрезки | Показатели | «Требует внимания» |
|---|---|---|---|---|
| «Дневники» | Р-88…Р-91 «Дневников», ядро `cb72d16` | все четыре | `illness.days`, `illness.episodes`, `training.count`, `training.minutes`, `training.km`, `content.started`, `content.done`, `content.dropped`; `content.monthOnly` — только недели. Свои коды: `no-duration`, `no-distance` | `illness.open`, `cycles.overdue`, `content.stale` |
| «Злотые» | Р-50 «Злотых», `2ce4193`, ядро `cb72d16` | месяцы; недели — `not-provided` | `month.records`, `recurring.waiting`, `capital.snapshots` — все `count`, без сумм. Свой код: `not-loaded` | `month.missing`, `recurring.waiting` |

- **Ключи «Дневников» постоянные**, ни один не из названий или id; разбивки тренировок по видам нет (Р-90 «Дневников»). Появится — ключ вида будет id тега: переименование не меняет, **слияние меняет** — пропавший ключ = слитый вид
- **Месяц «Злотых» календарный** (Р-50 «Злотых», п. 4): `periodStartDay` не читает ни один их расчёт. Наш открытый вопрос о не календарном месяце спит
- **`recurring.waiting` «Злотых»** — один ключ и у показателя, и у пункта «требует внимания»: списки разные, это не одна строка

**Для Этапа 3 — из ответа ядра и срезов**

- **Пары по ключам.** Настоящая — `training.*` «Дневников» и группа «Делу Время» про движение (`time.group.<ключ>` — название группы человека, ни в код, ни в документы). Слабая — учёт «Трапезы» и группа «Делу Время» про еду, если она есть. «Злотые» на ступени 1 пар не дают
- **`norm.*` — у «Делу Время» и «Трапезы»**: разные строки, различать по приложению
- **`kcal.perDay` «Трапезы»** — среднее хозяина (законно, Я-15 — про потребителя); «Делу Время» средних не отдаёт. Ставить ли среднее рядом с суммой — развилка Этапа 3
- **Минуты тренировок «Дневников» и минуты группы «Делу Время»** — две записи одного занятия в разных приложениях, с разными основаниями. «Рядом» — раскладка, не сумма и не разность (Я-15, Я-19, п. 5)

**Не держит Этап 3 — «небольшая сессия ядра»** (о ней писали «Дневники»): строки «Дневников» и «Злотых» в «Составе» и их «что в договоре не сошлось». «Дневники»: контент за неделю — точный счёт вместо `month-precision` (Р-89, отступление, выбор человека); `link` `illness.open` несёт id эпизода; свои коды — в список Я-17. «Злотые»: `through` один на отрезок, а свежесть показателей разная — **если ядро введёт `through` у показателя, это `format: 2` и правка нашего чтения и показа**; «ни одного дня не загружено» — свой код `not-loaded`. Остальное нас не задевает: «не известно» мы показываем словами хозяина.

**Следующий шаг** — `/clear`, сессия по `06-Процесс`, «Передача»: сначала указатель `cb72d16` и `noSync` (Я-29), потом развилки Этапа 3.

---

## 25.09.2026 — Этап 3 не начат: ждёт среза «Дневников» (Р-15); промпт сессии ядра

Сессия по «Передаче» под Этап 3. Ядро — `ee1dc37`, в `FamilyCore` новее нет. Кода нет.

**Сверено через `git show`:** срез отдают «Делу Время» и «Трапеза» (Р-55 «Трапезы», `9911c2d`); у «Дневников» и «Злотых» поля `summary` в конфиге нет. Человек: **«Трапеза» видна на «Сводке».**

**Находка.** Срез «Трапезы» групп и категорий не отдаёт (Р-55 «Трапезы») — учёт недели, ккал, нормы; месяцы — `not-provided`. С «Делу Время» пара для «рядом» одна и слабая: группа времени про еду рядом с учётом «Трапезы». Настоящая пара — тренировки «Дневников» и группа «Делу Время» про движение.

**Решение** — **Р-15**: Этап 3 ждёт среза «Дневников»; до него по порядку — сессия ядра, «Дневники», «Злотые». Выбор человека, рекомендация. План, `06`, «Передача» — под это.

**Находка для ядра:** строка «Трапезы» в таблице «Состав» ядра (`02`, «Срез итогов») — ещё не «отдаёт», и в ней `assumed`, которого в срезе нет.

### Сессия ядра — из корня `FamilyCore`

Заменяет промпт ядра из записи «Промпты» ниже.

```
Сессия ядра FamilyCore: итоги «Тотального Учёта», Этапы 0–2, и сверка
перед срезами «Дневников» и «Злотых».

Начни по CLAUDE.md, прочитай docs/. Материал — Журнал TotalUchet
(C:\Ф\Prognananda\Claude Projs\TotalUchet\docs\04-Журнал.md, записи
24–25.09.2026, «Для сессии ядра», «Промпты») и его Р-09, Р-12, Р-15 —
только читать, TotalUchet отсюда не правится. Репозитории приложений —
через git show, не файлами с диска.

1. Этап 3 ядра — закрыть: «Сводка» TotalUchet показала настоящий
   summary.json «Делу Время» на компьютере и телефоне, числа совпали
   с приложением; срез «Трапезы» там тоже виден. План, Журнал.
2. «Состав» в 02, «Срез итогов», строка «Трапезы» устарела: срез
   отдаётся — Р-55 «Трапезы», 9911c2d, ядро ee1dc37. Сверь с её 02,
   «Срез итогов», и перепиши семействами ключей со ссылкой, как у
   «Делу Время» (Я-21). Сейчас в строке assumed, которого в срезе нет;
   групп и категорий в срезе нет, месяцы — not-provided.
3. Промпты «Дневникам» и «Злотым» на срез выданы из TotalUchet
   (его Журнал, 25.09.2026, «Промпты») — твои промпты 24.09 с поправками
   Я-21…Я-27. Сверь с договором до их сессий; расходится — дай
   исправленный текст целиком. Для Этапа 3 TotalUchet («рядом», Я-19,
   п. 5) главное — тренировки «Дневников»: TotalUchet ставит строки
   рядом по key (Я-17, «Цена», п. 3), значит ключи тренировок —
   устойчивые, не из названий, которые человек переименовывает.
4. RepoInfo.canWrite (core/github.ts) — permissions.push из ответа GitHub.
   У fine-grained токена там, по всему видно, права аккаунта, а не токена:
   у владельца всегда true, токен Read-only показал «может писать»
   (Р-12 TotalUchet). Поправить комментарий; найти, где ядро опирается
   на canWrite (checkAccess, SyncSettings) и что видит человек у пишущего
   приложения с токеном Read-only. Меняется поведение — решение Я-NN.
5. sync в Core (ui/core.tsx) — необязательным; useSyncStatus без него —
   off. Тогда TotalUchet уберёт заглушку noSync (Р-09) обновлением
   указателя. Правка шва — Я-NN, если осмысленна для любого приложения.
6. На обсуждение, не на код, и не в ущерб пп. 1–5 — можно отдельной
   сессией: подключение устройства и друга «в пару кликов» (сейчас токен
   и репозиторий вписываются в каждое приложение руками, список
   приложений TotalUchet — файлом-копией); продление токенов разом или
   синхронизация без токенов руками. Рядом — открытый вопрос «токен один
   раз на устройство» (Я-25). По одной развилке с рекомендацией; решения —
   Я-NN или открытым вопросом.

В конце — Журнал, План. Отдельно: что нести в TotalUchet; что «Дневникам»
и «Злотым» — коммит ядра для указателя и промпты, если поправлены.
```

**Следующий шаг** — человек, по порядку: сессия ядра по промпту выше; «Дневники» и «Злотые» — по промптам из записи «Промпты» ниже или поправленным ядром; у каждого проверка — «Семья» → добавить → «Сводка». Потом сюда — по `06-Процесс`, «Передача».

---

## 25.09.2026 — Этап 2 закрыт: проверено на телефоне; Р-14

Человек: «всё работает, на телефоне проверил». **Android:** ссылки «Семьи» и «открыть» из «Зовут» открывают то установленное приложение, то вкладку браузера — какое когда, не отмечал; нужное приложение открывается. «В целом оба варианта приемлемы, у кого не установлено — откроется браузер». **iPhone не проверялся.**

**Р-14** — ссылки как есть, новой вкладкой; оба исхода годятся. Выбор человека. Цена — iPhone: вероятно Safari, сосед без данных; проверить, когда будет iPhone (План, «Хвосты», Этап 2).

План: Этап 2 снят. `06-Процесс`, «Передача» — под Этап 3; он ждёт второго среза.

**Следующий шаг** — `/clear`. Этап 3 начнётся, когда срез отдаст второе приложение: сначала сессии приложений по промптам ниже (запись «Промпты»), проверка — «Семья» → добавить → «Сводка». Потом — сессия отсюда по промпту `06-Процесс`, «Передача».

---

## 25.09.2026 — Этап 2: «Семья» — код сделан; ссылки на телефоне — за человеком

**Решение** — **Р-13**, записано в момент выбора, рекомендация, человек согласился: чтение общее со «Сводкой»; приложения в «Семье» — свёрнутыми блоками; «Как установить» — один блок. Вопрос человека — «установить получается сразу всё? нужен выбор» — ответ: одной кнопкой браузер не ставит ничего чужого (Р-07), каждое приложение ставится со своего сайта; поэтому внутри «Как установить» — ссылка на сайт каждого, выбор за человеком.

**Сделано**

- `ui/reading.tsx` — `ReadingProvider`: архив, проход чтения, «Обновить» — над маршрутами; `useReadOnOpen` — экран читает при открытии. «Сводка» переведена на него без изменения поведения
- `brief()` в `reading/refresh.ts` — итог у свёрнутого, один на оба экрана: «посчитано ДД.ММ», «срез не отдаёт», «не прочитан» (ошибкой); `isCalm` вынесен туда же. «Сводка»: у свёрнутого без среза теперь «срез не отдаёт», а не пусто
- «Семья»: приложение — `Fold`, внутри свежесть или `stateLine`, репозиторий, сайт, «Поправить» — форма в том же блоке, «Убрать». Нет токена — та же строка, что на «Сводке» (`screens/NoToken.tsx`). «Как установить» — свёрнут, итог «каждое — со своего сайта», внутри объяснение и «открыть сайт» у каждого
- Архитектура — «Семья», «Чтение среза», структура кода; «Что нового» — запись 5

*Проверено машинно:* `npm test` — 9 файлов, 64 теста; `npm run typecheck`; `npm run build`; `npm run smoke` — 54 проверки (новые: «не настроено» в «Семье», форма правки в блоке, три состояния словами, три итога у свёрнутых, «Как установить» свёрнут и ссылки на сайт каждого новой вкладкой); все с кодом 0. В `dist` нет ни подставного GitHub, ни выдуманного токена.

*Не проверено:* ссылки на телефоне — ниже.

### Проверка ссылок на телефоне — человек

Обе ссылки открываются новой вкладкой (`target="_blank"`). Вопрос Этапа 2: откроется **установленное приложение** на нужном экране или **вкладка браузера**. Ожидание сессии — по памяти, не проверено: на Android из вкладки — скорее вкладка; из установленного «Тотального Учёта» — вкладка Chrome или окно поверх; открыть установленного соседа страница не может. **На iPhone главный риск:** ссылка уйдёт в Safari, а у установленного соседа своё хранилище — откроется сосед без данных.

| Откуда открыт «Тотальный Учёт» | Ссылка | Android | iPhone |
|---|---|---|---|
| вкладка браузера | «Семья» → сайт приложения | | |
| вкладка браузера | «Сводка» → «Зовут» → «открыть» | | |
| иконка (установлен) | «Семья» → сайт приложения | | |
| иконка (установлен) | «Сводка» → «Зовут» → «открыть» | | |

В клетку — что открылось: установленное приложение / вкладка браузера / окно поверх; для «открыть» — тот ли экран; есть ли в открывшемся данные. «Зовут» виден, только если приложение сейчас зовёт.

**Не описано в документах — на обсуждение**

- Переход между «Сводкой» и «Семьёй» — новый проход чтения (Р-13, «Цена»). На пять приложений — десятки запросов; если станет заметно — читать не чаще раза в N минут, решением

**Следующий шаг**

1. Человек — таблица выше на телефоне; итог — сюда. Подтвердится «только вкладка» — решение, что делать со ссылками и подсказкой. Тогда Этап 2 закрыт, `06-Процесс`, «Передача» — под Этап 3
2. Этап 3 ждёт второго среза — сессии приложений по промптам ниже

---

## 25.09.2026 — Промпты: сессия ядра и срезы «Трапезы», «Дневников», «Злотых»

Вопрос человека: записано ли про ядро и срезы соседей, и промпты к ним. Сверено 25.09.2026: ядро — `ee1dc37`; срез пишет только «Делу Время» (`src/screens/summary.ts`), у «Трапезы», «Дневников», «Злотых» функции среза нет — все на ядре `6a5da3f`, код среза у них уже есть, не подключён. Промпты приложениям — промпты ядра (Журнал `FamilyCore`, 24.09.2026, «Промпты сессиям приложений») с поправками: Я-21…Я-27 после них, проверка — на «Сводке» здешнего приложения. Нового в договоре здесь не решено.

**Ничто из этого не держит Этап 2 здесь.** Этапу 3 («Рядом») нужен второй срез; самый полезный — «Дневники»: болезни и тренировки недели — факты соседей для обзора «Делу Время» (`01-Проект`, «Проблема»), и их тренировки встанут рядом с «Движением» «Делу Время». Быстрее всего — «Трапеза».

### Сессия ядра — из корня `FamilyCore`

```
Сессия ядра FamilyCore: итоги «Тотального Учёта», Этапы 0–1.

Начни по CLAUDE.md, прочитай docs/. Материал — Журнал TotalUchet
(C:\Ф\Prognananda\Claude Projs\TotalUchet\docs\04-Журнал.md, записи
24–25.09.2026, «Для сессии ядра») и его Р-09, Р-12 — только читать,
TotalUchet отсюда не правится.

1. Этап 3 ядра — закрыть: «Сводка» TotalUchet показала настоящий
   summary.json «Делу Время» на компьютере и телефоне, числа совпали
   с приложением. План, Журнал.
2. RepoInfo.canWrite (core/github.ts) — permissions.push из ответа GitHub.
   У fine-grained токена там, по всему видно, права аккаунта, а не токена:
   у владельца всегда true, токен Read-only показал «может писать»
   (Р-12 TotalUchet). Поправить комментарий; найти, где ядро опирается
   на canWrite (checkAccess, SyncSettings) и что видит человек у пишущего
   приложения с токеном Read-only. Меняется поведение — решение Я-NN.
3. sync в Core (ui/core.tsx) — необязательным; useSyncStatus без него —
   off. Тогда TotalUchet уберёт заглушку noSync (Р-09) обновлением
   указателя. Правка шва — Я-NN, если осмысленна для любого приложения.
4. На обсуждение, не на код — мысль человека: подключение устройства
   и друга «в пару кликов» (сейчас токен и репозиторий вписываются
   в каждое приложение руками, список приложений TotalUchet — файлом-копией);
   продление токенов разом или синхронизация без токенов руками. Рядом —
   открытый вопрос «токен один раз на устройство» (Я-25). По одной развилке
   с рекомендацией; решения — Я-NN или открытым вопросом.
5. Промпты «Трапезе», «Дневникам», «Злотым» на срез выданы из TotalUchet
   (его Журнал, 25.09.2026, «Промпты») — твои промпты 24.09 с поправками
   Я-21…Я-27. Сверь; расходится с договором — скажи, что поправить.

В конце — Журнал, План; что нести в TotalUchet.
```

### «Дневники» — из корня `Дневники\Dnevniki`

```
Сессия «Дневников»: срез итогов для метаприложения «Тотальный Учёт»
(ядро: Я-16…Я-21, Я-26).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-27 — особенно Я-19,
пп. 3–4 (ваши позиции, промежуточно) и Я-21 (закончившийся отрезок
не заморожен; нет пункта «требует внимания» — не «всё сделано»).
src/shared не правится; договор мешает — скажи мне, это сессия ядра.
Сначала план работ, код после подтверждения.

1. git submodule update --remote src/shared — до текущего ядра;
   npm test, tsc (typecheck или build), smoke — зелёные по коду выхода,
   коммит указателя.
2. Новые расчёты, каждый — своим решением Р-NN «Дневников», чистой
   функцией с тестами:
   - дни болезни в промежутке — пересечение эпизодов с промежутком,
     наложения склеены, открытый длится до дня расчёта;
   - contentStats по Period ядра — начато, досмотрено, брошено;
     «досмотрено» — нынешний статус начатого в промежутке; плюс «ещё N
     с точностью до месяца»; месячную дату — никогда первым числом.
3. Чистая функция summary(data, day), отрезки — summaryPeriods(day).
   Состав (промежуточно): дни болезни и счёт эпизодов — без названий,
   симптомов, заметок; тренировки — счёт, минуты и км с оговоркой
   в основании; контент по промежутку, неделя — { unknown:
   'month-precision', text }. Измерения — не в срез. Никогда: название
   эпизода, note, симптомы, comment. Доля, если будет, — share от 0 до 1,
   не проценты (Я-26).
4. «Требует внимания»: открыт ли эпизод (day — день начала), число
   просроченного в циклах, число зависшего в «смотрю» — всё с basis,
   без названий позиций. link — путь хеш-роутинга нужного экрана.
5. Цены отметок циклов в срез не идут: их никто не просит.
6. Поле summary в конфиге; checkSummary(buildSummary(...)) в тестах.
7. После синхронизации — summary.json в репозитории данных, читается
   parseSummary. Проверка глазами: «Тотальный Учёт» → «Семья» → добавить
   «Дневники» (репозиторий данных, сайт) → «Сводка». Подпись и основание
   видны как есть, без контекста приложения: строка должна читаться сама.

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что
в договоре не сошлось.
```

### «Трапеза» — из корня `Еда\Trapeza`

```
Сессия «Трапезы»: срез итогов для метаприложения «Тотальный Учёт»
(ядро: Я-16…Я-21, Я-26).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-27 — особенно Я-19,
пп. 1–2 и Я-21. src/shared не правится; договор мешает — скажи мне, это
сессия ядра. Сначала план работ, код после подтверждения.

1. git submodule update --remote src/shared — до текущего ядра;
   npm test, tsc (typecheck или build), smoke — зелёные по коду выхода,
   коммит указателя.
2. Чистая функция summary(data, day) с тестами, отрезки — summaryPeriods(day).
   Недели (промежуточно, Я-19): основание — logged из elapsed; portions,
   records, loose/looseRecords, assumed; ккал с основанием KcalSum
   и среднее на день учёта; нормы — вердиктом met/failed/open,
   в основании — правило minDays/maxDays. Названия групп и категорий —
   только если решишь, что они в составе (Я-14); Intake.note и рецепт —
   никогда. Доля, если будет, — share от 0 до 1 (Я-26).
   Месяцы — metrics: { unknown: 'not-provided', text }: месяц — новый
   расчёт и твоё отдельное решение, не сейчас (Я-19, п. 1).
3. «Требует внимания» — вчерашние пропуски приёмов только по записям,
   без отметок «не было» (они в settings, срез их не видит, Я-16):
   count — число пропущенных приёмов, day — вчера, basis — «по записям;
   отметки „не было“ не учтены» (Я-19, п. 2; Я-20). link — путь экрана.
   «Сегодня пусто» остаётся твоему напоминанию.
4. Поле summary в src/app/config.ts; в тестах — checkSummary(buildSummary(...)).
5. Решение Р-NN «Трапезы»: ключи, подписи, основания среза.
6. После синхронизации — summary.json в репозитории данных, читается
   parseSummary. Проверка глазами: «Тотальный Учёт» → «Семья» → добавить
   «Трапезу» → «Сводка»: подпись и основание читаются сами, без экрана
   «Трапезы» рядом.

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что
в договоре не сошлось.
```

### «Злотые» — из корня `Злотые`

```
Сессия «Злотых»: срез итогов для метаприложения «Тотальный Учёт»
(ядро: Я-16…Я-21, Я-26).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-27 — особенно Я-13
(ваши три ступени), Я-21 и Я-26. src/shared не правится; договор мешает —
скажи мне, это сессия ядра. Сначала план работ, код после подтверждения.

1. git submodule update --remote src/shared — до текущего ядра;
   npm test, npm run typecheck, build, smoke — зелёные по коду выхода,
   коммит указателя.
2. Чистая функция summary(data, day) с тестами, отрезки — summaryPeriods(day).
   Только ступень 1 Я-13, без сумм: есть ли записи за месяц; through
   у отрезка месяца — по какое число доведены выписки (recordedThrough);
   сколько регулярных ждёт; день последнего снимка капитала. Недели —
   { unknown: 'not-provided', text }. Ступени 2 и 3 не просит ни один
   потребитель — не отдавать. Имена людей — никогда.
3. Если ваш «месяц» с periodStartDay не календарный — отдай его концы
   как есть: ядро месяц не проверяет (Я-12). Скажи мне об этом отдельно:
   «Тотальный Учёт» ищет отрезок по концам, и не календарный месяц у него
   покажется «не известно» — правило сопоставления решается, когда такой
   срез появится.
4. «Требует внимания»: за прошлый месяц нет записей, регулярные ждут —
   с basis (Я-20), link — путь экрана.
5. Склейку итога месяца из Month.tsx в чистую функцию выносить сейчас
   не нужно: она понадобится для ступеней 2–3. Запиши это в своём Плане.
6. Поле summary в конфиге; checkSummary(buildSummary(...)) в тестах;
   после синхронизации — summary.json в репозитории данных. Проверка
   глазами — «Тотальный Учёт» → «Семья» → добавить «Злотые» → «Сводка».

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что
в договоре не сошлось.
```

---

## 25.09.2026 — Этап 1 закрыт: проверено на телефоне

Человек: «всё работает, на телефоне всё сделал» — токен и «Делу Время» вписаны на телефоне, «Сводка» показывает настоящий срез, без сети — последний прочитанный. На компьютере числа совпали с экраном «Делу Время» (запись ниже). Условие «Готово когда» Этапа 1 выполнено; План: Этап 1 снят.

**Для сессии ядра** — список в записи ниже, «Для сессии ядра», пп. 1–4. Первым — **Этап 3 ядра закрыт** «Сводкой» на настоящем срезе. Сессия ядра здешнюю работу не держит: Этапу 2 ядро не нужно, Этап 3 ждёт срезов соседей — это сессии приложений.

**Следующий шаг** — `/clear`; Этап 2 по промпту `06-Процесс`, «Передача».

---

## 25.09.2026 — Этап 1: настоящий срез «Делу Время» на «Сводке» — числа совпали; Р-12

**Проверено человеком на компьютере:** токен чтения выпущен (Contents: Read-only), «Делу Время» добавлено в «Семье»; «Сводка» показала настоящий `summary.json` — строки, основания, «посчитано · по записям по», «идёт, по»; **числа совпали с экраном «Делу Время»**. Этим выполнено условие Этапа 3 ядра — «первый экран читает настоящий `summary.json`».

**Находка — ложное «токен шире, чем чтение».** У токена с Read-only горело предупреждение: `canWrite` из `info()` ядра — `permissions.push` ответа GitHub, а у fine-grained токена там, по всему видно, права аккаунта, у владельца — всегда `true`. **Р-12** — предупреждения нет; «Проверить доступ» показывает, какой репозиторий видит токен, и что права токена GitHub не сообщает. Р-11, п. 2 (новый `readAt`) — в силе. Человек согласился.

**Телефон.** Приложения, добавленные на компьютере, на телефоне не появились — так и задумано (Р-02): список и токен у каждого устройства свои, перенос — файлом-копией или руками. Человек: перенос сделать автоматичнее — в План, «Отложено», вместе с «подключением в пару кликов» и продлением токенов.

**Сворачивание** (просьба человека: всё уместное — сворачиваемым, на всех экранах). «Сводка»: «Зовут» и блок каждого приложения — `Fold` ядра; у свёрнутого — кто зовёт по именам приложений (не число пунктов: сумма между приложениями — уже почти досчёт, Я-15) и «посчитано ДД.ММ» или «не прочитан». «Настройки» уже все на `Fold`. «Семья» — короткий список и форма, сворачивать нечего.

*Проверено машинно после Р-12 и сворачивания:* `npm test` — 59; `npm run typecheck`; `npm run build`; `npm run smoke` — 43 проверки; все с кодом 0.

**Для сессии ядра — человек несёт в `FamilyCore`**

1. **Этап 3 ядра можно закрывать:** «Тотальный Учёт» показал настоящий `summary.json` «Делу Время», числа совпали с приложением
2. **`RepoInfo.canWrite` врёт для fine-grained токенов** (Р-12 „Тотального Учёта“): комментарий «есть ли у токена право писать» неверен — это права аккаунта. «Проверить доступ» у приложений, вероятно, говорит то же; токен с Read-only там не виден до первой отправки
3. **Подключение устройства и друга «в пару кликов», продление токенов разом или синхронизация без токенов руками** — мысль человека, общая для семьи; рядом — открытый вопрос «токен один раз на устройство» (Я-25)
4. `sync` в `Core` необязательным — в силе

**Следующий шаг** — человек: телефон — токен и «Делу Время» (руками или копией), «Сводка», затем без сети. Итог — сюда; тогда Этап 1 закрыт, `06-Процесс`, «Передача» переписывается под Этап 2.

---

## 25.09.2026 — Этап 1: код «Сводки» сделан; проверка на настоящем срезе — за человеком

**Решения** — записаны в момент выбора, рекомендации, человек согласился:

- **Р-10** — «Семья» на Этапе 1: добавить, поправить, убрать; порядок — числом в форме. Иначе опечатку в имени репозитория не исправить. План: Этап 2 — состояние среза в «Семье», «Установить», ссылки на телефоне
- **Р-11** — `canWrite` ещё и строкой на «Сводке»; тот же `sha` — новый `readAt` у последнего увиденного

**Сделано**

- `view/`: отрезки экрана — `summaryPeriods(сегодня)` ядра; отрезок среза ищется по зерну и **обоим** концам (месяц с тем же началом, но другим концом — не наш, Я-12); свежесть; значение словами — минуты «5 ч 40 мин», доля «25 %» с «−», деньги по числу знаков валюты из `Intl`, вердикт словами без цвета; «Зовут» — пустое «не зовут» только если увиден хоть один срез
- `reading/`: чтение по Р-05 — `info()`, голова ветки по умолчанию, дерево, только `summary.json`, сверка `sha`, `parseSummary`. Свои тексты: 401, 404 на доступе, 403 после доступа («видит, но не читает: нужен Contents: Read-only»), лимит, нет связи. Обрыв связи узнаётся на самом `fetch`: статус 0 у клиента ядра бывает и без сервера. Архив `seen` по `seenId`; проверка доступа для «Настроек»
- «Настройки» → «Токен чтения»: вписать, заменить, забыть, проверить доступ по каждому приложению; токен на экран не выводится, раздел открыт, пока токена нет
- «Семья» по Р-10: репозиторий — «владелец/имя» через `parseRepo` ядра, ссылку можно вставить целиком; сайт — `http(s)`, хвост экрана отбрасывается, слеш дописывается
- «Сводка»: сразу — архив, затем каждое приложение читается само; «Обновить»; «Зовут»; переключатель из четырёх отрезков; блок приложения — свежесть, состояние чтения, «идёт, по», строки в порядке хозяина с основанием
- Строки «Что нового» — запись 2

*Проверено машинно:* `npm test` — 9 файлов, 59 тестов; `npm run typecheck`; `npm run build`; `npm run smoke` — 39 проверок; все с кодом 0. Прогон перехватывает запросы к `api.github.com` через CDP `Fetch` и отвечает подставным GitHub тестов с выдуманным срезом «Полки»: три приложения в «Семье», токен и проверка доступа, строки и основания, «Зовут» со ссылкой, все четыре отрезка сверены с функциями показа, «срез не отдаёт», «токен не видит репозиторий», без связи — «прочитан …, сейчас нет связи». В сборке нет ни подставного GitHub, ни выдуманного токена — проверено поиском по `dist`.

*Не проверено:* настоящий `summary.json` «Делу Время» и токен человека; телефон; «открыть» из «Зовут» на телефоне — Этап 2.

**Находки по ходу**

- **Клиент ядра даёт статус 0 не только обрыву связи** — ещё «дерево не поместилось» и чужой кодировке. «Нет связи» узнаётся обёрткой `fetch` в `read.ts`
- **403 после успешного `info()`** — ожидаемо у токена, выпущенного на репозиторий без Contents: Metadata у fine-grained токена есть всегда, и доступ «виден». На настоящем GitHub не проверено

**Для сессии ядра**

- Этап 3 ядра закроется, когда человек увидит на «Сводке» настоящий срез «Делу Время» (План, Этап 1, п. 6). Пункт «`sync` в `Core` необязательным» — в силе

**Не описано в документах — на обсуждение**

- **Срок токена чтения не виден.** У пишущих приложений ядро предупреждает за 30 дней (`WARN_DAYS`), здесь — только «токен не принят» после истечения. Показывать ли дату из заголовка GitHub в «Настройках» — решением
- **Подписи отрезков** — «Эта неделя», «Прошлая неделя», «Прошлый месяц», «Этот месяц»; в документах — «идущая». Под переключателем — даты отрезка словами
- **«открыть» и ссылка на сайт** — новой вкладкой (`target="_blank"`); как это ведёт себя у установленного приложения — Этап 2

**Следующий шаг**

1. Человек: выпустить токен чтения (`src/shared/docs/06-Процесс.md`, «Токены семьи», последний абзац), вписать в «Настройках», в «Семье» добавить «Делу Время» — имя, репозиторий данных, сайт; «Проверить доступ»; сверить строки «Сводки» с экраном «Делу Время» за ту же неделю; на телефоне — то же и без сети
2. Итог — сюда; при совпадении Этап 1 закрыт, и это же — закрытие Этапа 3 ядра для его сессии

---

## 24.09.2026 — Этап 0 закрыт: проверено на телефоне

Человек: «всё работает, в том числе на телефоне» — установка иконкой, работа без сети, обновление после деплоя. Заглушку синхронизации (Р-09) и пункт ядру «`sync` в `Core` необязательным» — принял. План: Этап 0 снят, хвост — заглушка до правки ядра.

**Токены — ответ на вопрос человека.** Метаприложению сейчас нужен **один** новый токен — только на чтение (Я-16, Я-27), его выпуск — начало Этапа 1. Токен «семья» с записью — у четырёх приложений, сюда не вписывается никогда (Я-25). Второй токен самого метаприложения — с записью в свой репозиторий данных — только если понадобится синхронизация архива и списка приложений (Р-02, Р-08; План, «Отложено»); сейчас его нет.

**Следующий шаг** — `/clear`; Этап 1 по промпту `06-Процесс`, «Передача».

---

## 24.09.2026 — Вторая сессия: документы под ответ ядра (Я-23…Я-27); Этап 0

**Документы под ответ ядра** (Журнал `FamilyCore`, «итоги первой сессии „Тотального Учёта“» и «Я-27»; ядро на `ee1dc37`): `02` — `share` — доля (Я-26), заглушки импорта и `dbName` подтверждены (Я-23), токен чтения — один на все устройства (Я-27); `05` — открытый вопрос «Единица `share`» снят: Я-26; `06` — токен ссылкой на Я-27, «Передача» — промпт ядра; План — снято выполненное «до кода Этапа 0». Р-02 про «свой на каждом устройстве» не говорит — новой записи не нужно, Я-27 правит только `06`.

**Решение** — **Р-09**: `CoreProvider` ядра требует `sync`, а синхронизации нет (Я-23) — ему своя заглушка `noSync` «выключено»; `createSync` не вызывается. Рекомендация, человек согласился.

**Этап 0 — код сделан, этап не закрыт: осталась проверка на телефоне**

- Ядро подмодулем на `ee1dc37`; `package.json` — версии как у «Делу Время»; `familyVite`, `tsconfig` через `extends`; тесты ядра исключены из своего vitest
- `src/app/`: `App`, `Seen` дословно из Архитектуры, `seenId` — `${app}:${computedOn}` (Р-08); `dbName` `totaluchet`, `places` — `apps.json` и `seen/ГГГГ-ММ.json` по `computedOn`, индекс `seen` по `app`; заглушки импорта (Я-23). **С первым деплоем `v1Stores`, `indexes`, `places` заморожены** (Я-08, Я-09)
- Экраны: «Сводка» и «Семья» — каркас с подсказкой, что будет; «Настройки» шестерёнкой — «Название» (Р-03), «Копия данных» файлом, «О приложении» (дата сборки, установка, «Что нового», отчёт об ошибке, счёт записей)
- Своё название (Р-03): имя по умолчанию — `src/ui/title.ts`, оттуда же `config.ts`, `vite.config.ts` и прогон; тест-сторож — названия нет в исходниках вне `title.ts`, `<title>` и описание `index.html` совпадают с ним
- Иконка — четыре квадрата в акцентах соседей и бирюзовый кружок в середине, выбор человека; акцент интерфейса — бирюзовый
- `deploy.yml`: подмодуль, `npm ci`, test, build, Pages. Pages → Source → GitHub Actions включил человек. Первый деплой прошёл, сайт отдаёт страницу, манифест, работника, иконки

*Проверено машинно:* `npm test` — 3 файла, 16 тестов; `npm run typecheck`; `npm run build`; `npm run smoke` — 17 проверок, в том числе своё название после перезапуска, скачанный файл копии, отчёт об ошибке на заглушке, открытие без сети из кеша работника; все с кодом 0. Прогон падает с кодом 1 на испорченной строке «Семьи» — проверено. Деплой на GitHub Actions — зелёный.

*Не проверено:* установка на телефоне, имя под иконкой, без сети на телефоне, доезжает ли обновление до установленного — это человек (План, Этап 0).

**Находки по ходу**

- **`Core` ядра требует `sync`** → Р-09 и пункт ядру ниже
- **`@types/node` в семье нет** — тест-сторож читает исходники через `import.meta.glob` с `?raw`: типы Vite уже подключены
- **Прогон берёт имя из `title.ts` напрямую** — Node 24 читает TypeScript без сборки
- **`gh` на этом компьютере — fine-grained токен без права Pages**: включить Pages из `gh api` нельзя (403). Смотреть workflow — можно

**Для сессии ядра**

1. **`sync` в `Core` (`ui/core.tsx`) — необязательным.** Приложение без синхронизации (метаприложение, Р-09 „Тотального Учёта“) иначе держит заглушку из восьми методов; `useSyncStatus` при её отсутствии — `off`. Не срочно: заглушка работает
2. **Этап 3 ядра** — ещё не закрыт: закроется Этапом 1 здесь

**Не описано в документах — на обсуждение**

- Имя под иконкой: `shortName` не задан, под иконкой — полное имя. Обрежется на телефоне — решением

**Следующий шаг**

1. Человек — телефон по Плану, Этап 0; итог — сюда, в Журнал, следующей сессией
2. `/clear`; сессия по промпту `06-Процесс`, «Передача»: закрыть Этап 0 по итогам телефона, потом Этап 1. Для Этапа 1 человек выпускает токен чтения (`src/shared/docs/06-Процесс.md`, «Токены семьи», последний абзац)

---

## 24.09.2026 — Первая сессия: документы и решения Р-01…Р-08; кода нет

Вход — стартовый промпт ядра (Журнал `FamilyCore`, 24.09.2026, «Я-22…», окончательный). Прочитано только на чтение: `FamilyCore` — `CLAUDE.md`, `01`, `02` («Договор семьи», «Срез итогов»), `05` Я-10…Я-22 и открытые вопросы, `04` записи 24.09.2026, `06`, `07`; код `core/summary.ts`, `model.ts`, `github.ts`, `sync.ts` (`checkAccess`), `ui/Layout.tsx`, `scripts/vite.ts`. «Делу Время» на `c0ffcc2` через `git show` — `02` «Срез итогов», Р-86, Р-87, Р-26.

**Решения — все записаны в момент выбора**

- **Р-01 — репозиторий кода публичный.** Ни токенов, ни настоящих срезов, ни названий групп; логи Actions без содержимого срезов. Вопрос человека, рекомендация
- **Р-02 — ядро подмодулем; своей синхронизации нет; свои записи с `Base`; один токен только на чтение.** Синхронизация своих записей — второй токен: у fine-grained токена одни права на все выбранные репозитории
- **Р-03 — «Тотальный Учёт»; своё название — настройка устройства.** Выбор человека; приём — Р-26 «Делу Время»
- **Р-04 — «Сводка»: отрезок переключателем, по умолчанию идущая неделя; отрезок среза ищется по концам; строки в порядке хозяина.** Человек выбрал идущую неделю вместо рекомендованной прошлой
- **Р-05 — чтение: сначала доступ, потом файл; состояние словами у каждого; без сети — последний увиденный**
- **Р-06 — «Зовут» наверху «Сводки»; уведомления нет** до решения ядра по Я-18
- **Р-07 — «Семья» без отметки «установлено»**
- **Р-08 — архив увиденных срезов на устройстве; экрана и синхронизации пока нет.** Человек: «Б, но может и В — хочется видеть динамику». Решено Б так, чтобы В добавилась без миграции

**Сделано**

- `CLAUDE.md`, `docs/01…06` — по шаблонам `doc-driven-project`

**Находки по ходу**

- **Отрезки среза считаются от `computedOn`, а не от сегодня.** Срезы разных приложений, посчитанные в разные дни, называют «прошлой неделей» разные недели → поиск отрезка по концам (Р-04)
- **Приватный репозиторий без доступа GitHub отдаёт тем же 404, что и несуществующий** → доступ проверяется до файла (Р-05)
- **У fine-grained токена один набор прав на все выбранные репозитории** → синхронизация своих записей несовместима с «только на чтение» одним токеном (Р-02)
- **`getInstalledRelatedApps` для соседей** — только Chrome на Android, `assetlinks.json` в корне origin, первые три приложения из манифеста (Р-07)
- **Тексты ошибок клиента GitHub ядра написаны для пишущего:** 403 — «нужен доступ Read and write». Для читателя тексты свои, по коду ответа. `info()` отдаёт `canWrite` — годится для предупреждения «токен шире, чем чтение»
- **Названия групп «Делу Время» — в ключах среза** (`time.group.<ключ>`) → в публичный репозиторий не попадают даже они (Р-01)

**Вопрос человека: можно ли обойтись одним токеном на всю семью** — ответ сессии, не решение (решает человек и сессия ядра):

- метаприложению токен и так один (Р-02)
- приложениям сейчас — по токену с записью в свой репозиторий на каждом устройстве. **Без переработки кода** можно выпустить один fine-grained токен «семья» с «Read and write» на все репозитории данных и вписать его во все четыре приложения: каждое проверяет только свой репозиторий. Итого два токена — «семья» и чтение для метаприложения — вместо пяти
- **Один на всё** — только если дать метаприложению запись: нарушает Я-16; ошибка метаприложения перестаёт быть безопасной для данных. Не советую
- Цена общего токена: истёк или отозван — встают все приложения разом; утечка — запись во все репозитории. Смягчает то, что граница и сейчас держится правилом, а не браузером: все приложения на одном origin, и скрипт любого из них технически может открыть базу соседа с его токеном
- **Вписать токен один раз на устройство для всех приложений** — правка ядра: общее хранилище на origin. Не переработка, но договор («Устройство и origin»). Вопрос сессии ядра
- **Один репозиторий данных на всех** — переработка, отвергнута Я-16

**Человек выбрал первый путь: токен «семья» с записью для приложений и отдельный токен чтения для метаприложения.** Перепроверено по коду ядра (`core/sync.ts`, `core/layout.ts`):

- *Плюсы:* выпуск и продление — один раз, срок жизни — одна дата; новое приложение, в том числе «Дача», — правкой списка репозиториев того же токена; код не меняется
- *Минус, которого при отдельных токенах нет:* **перепутанный репозиторий данных.** Сейчас приложение с чужим именем репозитория получает 404 и стоит. С общим токеном доступ есть, а `meta.json` хранит только `schemaVersion` — чьи это данные, `parseMeta` не проверяет. Если схема соседа не новее, проход отправит в чужой репозиторий свои файлы хранилищ, `meta.json` со своей версией и `summary.json` поверх соседского. Итог: срез соседа подменён до его следующего прохода; сосед может остановиться на «обнови приложение»; при совпадении имён хранилищ сосед вольёт чужие записи как свои
- *Минусы общие:* утечка — запись во все репозитории; истёк или отозван — встают все разом (ядро предупреждает за 30 дней, `WARN_DAYS`, в каждом приложении)
- *Смягчения:* **токен «семья» — свой на каждом устройстве**, как токен чтения по Я-16: потерянный телефон отзывается один. Имя репозитория сверять кнопкой «Проверить доступ» — она показывает полное имя. **В ядре — защита от чужого репозитория** (список для ядра, п. 6); переходить на общий токен лучше после неё

**Состояние: документы написаны, кода нет**

*Проверено машинно* — нечего: кода нет.

*Не проверено* — всё; План, Этапы 0–2.

**Для сессии ядра — человек несёт в `FamilyCore`**

1. **`dbName` `totaluchet`** — в договор, «Устройство и origin»; в `01-Ядро`, «Кто берёт ядро»: метаприложение берёт ядро (Р-02 „Тотального Учёта“). **До кода Этапа 0**
2. **Единица `share`** — доля от 0 до 1 или проценты: договор не говорит
3. **Поля импорта `AppConfig` обязательны** (`importFormat`, `promptRules`, `about.sources`), а у метаприложения импорта нет — сделать необязательными или оставить заглушки
4. **Тексты ошибок 403/404 клиента GitHub** — для пишущего; читателю нужны свои. Не мешает: у нас свои тексты
5. **Своё название приложения** — у «Делу Время» есть переименование вкладок (Р-26 „Делу Время“), у «Тотального Учёта» будет имени (Р-03). Если хочется во всей семье — параметр ядра
6. **Токен «семья»** — практика, которую выбрал человек (выше): записать в документы ядра. **До перехода на него — защита от чужого репозитория:** `meta.json` называет приложение, проход отказывается писать в репозиторий другого приложения; давний `meta.json` без имени принимается, и имя дописывается. Правка договора — Я-NN. «Вписать токен один раз на устройство для всех приложений» — отдельный вопрос, правка ядра
7. **Экран динамики по архиву** требует пересмотра Я-15 до кода (Р-08)
8. **Этап 3 ядра закроется Этапом 1 здесь** — «Сводка» на настоящем `summary.json` «Делу Время»

**Не описано в документах — на обсуждение**

- Месяц, который не календарный (Я-12), — правило сопоставления; когда появится первый такой срез (`05`, открытые вопросы)
- Сопоставление групп соседей — Этап 3

**Следующий шаг**

1. Сессия ядра — пункты 1 и 6 списка выше: `dbName` до кода Этапа 0, защита от чужого репозитория до перехода на токен «семья»; остальные — по мере надобности
2. `/clear`; сессия Этапа 0 отсюда — промпт в `06-Процесс`, «Передача в следующую сессию»
