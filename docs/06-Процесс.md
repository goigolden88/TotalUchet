# Процесс

Обновлён: 25.09.2026 (Р-23…Р-29)

Как ведётся работа над метаприложением. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код и документы | `github.com/goigolden88/TotalUchet`, публичный (Р-01). Локально — `C:\Ф\Prognananda\Claude Projs\TotalUchet` |
| Инструкции для Claude Code | `CLAUDE.md` в корне |
| Ядро семьи и договор | `src/shared/` — подмодуль `FamilyCore`; локально ещё `Claude Projs\FamilyCore`. **Отсюда не правится** |
| Приложения семьи | «Делу Время» `ТаймМенеджмент\DeluVremya`, «Трапеза» `Еда\Trapeza`, «Дневники» `Дневники\Dnevniki`, «Злотые» `Злотые` — **только читать**, через `git show` |
| Срезы | `summary.json` в приватных репозиториях данных приложений — читаются только приложением «Тотальный Учёт» в браузере, токеном из его настроек. В репозиторий, в чат и в логи не копируются (Р-01) |
| Токен чтения | `settings` каждого устройства; выпускает человек — **один на все его устройства** (Я-27), «Contents: Read-only» на репозитории данных семьи (Р-02). Выпуск по шагам — `src/shared/docs/06-Процесс.md`, «Токены семьи», последний абзац. У приложений семьи — отдельный токен «семья» с записью (Я-25, Я-27); сюда он не вставляется никогда |
| Список приложений и архив | IndexedDB устройства, `dbName` `totaluchet`; перенос — файлом-копией (Р-02, Р-08) |
| Телеграм-бот | код — здесь, `src/bot/`, `scripts/bot.mjs`; запуск — workflow приватного репозитория человека, там же `bot.json` и секреты (Р-23, Р-28). Заводится по разделу «Телеграм-бот: как завести» ниже |
| Токен чтения бота | свой, в секрете `READ_TOKEN` приватного репозитория; выпуск и продление — в один день с токеном чтения метаприложения (Я-34) |

### Сессии Claude Code

Запускать из корня `TotalUchet`: история бесед привязана к рабочей папке. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### Ядро — подмодулем

- **Клон** — только `git clone --recurse-submodules`; уже склонированное — `git submodule update --init`. Иначе `src/shared` пустой, сборка падает непонятно
- В `deploy.yml` у `actions/checkout` — `submodules: true`
- **В `src/shared` не запускать `npm install`** — появится вторая копия React (Я-02)
- **Обновление ядра** — `git submodule update --remote src/shared`, тесты, сборка, прогон, коммит указателя. Перед пушем `git status` чистый **и в подмодуле**: `git -C src/shared status`
- **Нехватка в ядре или противоречие с договором** — не правка `src/shared`, а слово человеку: это сессия ядра, решение Я-NN. Потом — обновление указателя здесь

### Сессия ядра — что ей отсюда передаётся

Правки договора — формы, состава, правил чтения — решает только сессия `FamilyCore`. Отсюда — список в Журнале, раздел «Для сессии ядра», человек несёт его туда.

## Телеграм-бот: как завести

Сводка недели по понедельникам и месяца 1-го числа — в личный чат (Р-23…Р-29). Код — в этом репозитории; запускает его workflow **приватного** репозитория человека: там `bot.json` и секреты, а логи не видны чужим. На «Настройки» → «Телеграм-бот» ведёт ссылка сюда — заголовок раздела не менять.

1. **Бот.** В Телеграме — @BotFather → `/newbot`: имя, адрес бота. Токен, который он выдаст, — секрет `TELEGRAM_TOKEN`. Там же `/setjoingroups` → бот → **Disable**: в группу его не добавить (Р-25)
2. **Id чата.** Напиши своему боту `/start`. В браузере открой `https://api.telegram.org/bot<токен бота>/getUpdates` — в ответе `"chat":{"id":<число>,…,"type":"private"}`. Число — секрет `TELEGRAM_CHAT`. Токен побывал в адресной строке — удали этот адрес из истории браузера
3. **Токен чтения бота** (Я-34) — `github.com/settings/personal-access-tokens/new`: имя «бот», Repository access — Only select repositories — репозитории данных приложений семьи, Permissions — Contents: **Read-only**; срок — **тот же день**, что у токена чтения «Тотального Учёта». Секрет `READ_TOKEN`. Копию вне GitHub не хранить
4. **Приватный репозиторий** — любое имя, Private. «Настройки» метаприложения → «Телеграм-бот» → «Скачать список для бота»; в репозитории — Add file → Upload files → `bot.json` в корень
5. **Секреты** — Settings → Secrets and variables → Actions → New repository secret: `READ_TOKEN`, `TELEGRAM_TOKEN`, `TELEGRAM_CHAT`. На вкладке Variables — `TZ`, свой часовой пояс, например `Asia/Yekaterinburg`: от него «сегодня» бота
6. **Workflow** — файл `.github/workflows/bot.yml` из образца ниже. Время в `cron` — UTC: 09:00 по +05:00 — это `0 4`. Другой пояс — поправь часы в обеих строках и в условии `BOT_PERIOD`
7. **Проверка** — Actions → «Телеграм-бот» → Run workflow → отрезок. Через минуту — сообщение в Телеграме. В логе — только «Бот: отправлено частей: N» или причина словами
8. **Превью на экране блокировки** показывает начало сообщения — дни болезни, названия групп. Скрыть — настройкой Телеграма: Настройки → Уведомления и звуки → Личные чаты → «Показывать текст» выключить

**Дальше:**

- поменял приложения в «Семье» — снова «Скачать список для бота» и Upload поверх старого `bot.json`
- продление — в один день с токеном чтения метаприложения: у GitHub — Regenerate, новое значение — в секрет `READ_TOKEN`. За 30 дней бот сам напишет в сообщении, что токен скоро истекает (Р-29)
- бот упал до Телеграма — неверный токен бота, чат не личный, нет сети — GitHub пришлёт письмо об упавшем workflow
- утёк токен бота — @BotFather → `/revoke`, новый — в `TELEGRAM_TOKEN`

**Образец `.github/workflows/bot.yml`:**

```yaml
name: Телеграм-бот

# Сводка «Тотального Учёта» в личный чат (его Р-23…Р-29). Код — публичный
# TotalUchet с main, ядро подмодулем; здесь — только bot.json и секреты.
# Лог — «отправлено частей: N» или причина словами: ни среза, ни сообщения,
# ни токенов (Я-33).

on:
  schedule:
    - cron: '0 4 * * 1' # понедельник 09:00 по +05:00 — прошлая неделя
    - cron: '0 4 1 * *' # 1-е число 09:00 по +05:00 — прошлый месяц
  workflow_dispatch:
    inputs:
      period:
        description: Отрезок
        type: choice
        options: [lastWeek, lastMonth]
        default: lastWeek

permissions:
  contents: read

jobs:
  send:
    name: Сводка
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # Этот репозиторий — ради bot.json.
      - uses: actions/checkout@v4
        with:
          path: own

      # Код бота. Оба репозитория публичны — токен не нужен.
      - uses: actions/checkout@v4
        with:
          repository: goigolden88/TotalUchet
          ref: main
          submodules: true
          path: app

      # Node 24 читает TypeScript без сборки: npm ci не нужен.
      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - run: node app/scripts/bot.mjs
        env:
          READ_TOKEN: ${{ secrets.READ_TOKEN }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT: ${{ secrets.TELEGRAM_CHAT }}
          BOT_LIST: own/bot.json
          BOT_PERIOD: ${{ inputs.period || (github.event.schedule == '0 4 1 * *' && 'lastMonth' || 'lastWeek') }}
          TZ: ${{ vars.TZ }}
```

## Разделение работы

**Claude Code — и «что строить», и «как».** Он видит настоящие файлы ядра и приложений и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой.** Из чата сюда приходят готовые куски markdown и список того, что переделать в коде. Настоящие срезы в чат не носить.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run typecheck`, сборка, прогон** — итог по коду выхода, а не grep'ом по выводу
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **План содержит только предстоящее**
- **Правки файлов с кириллицей** — инструментом правки или скриптом-файлом из папки сессии, не heredoc Bash: он съедает `\n` и `\` (опыт «Трапезы» и ядра)
- **Чужие репозитории — только `git show`**, не файлы с диска: рабочие копии в CRLF врут

## Шаблоны

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что>. Прочитай CLAUDE.md и docs/. Код не пишем — обсуждаем
по одной развилке, с твоей рекомендацией, и записываем решения в docs/
в момент выбора. В конце — Журнал, передача, коммит документов.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах и что нести в сессию ядра.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Сейчас — Этапы 0–3 закрыты; Этап 4 — код сделан, ждёт проверки человеком.

```
Проект «Тотальный Учёт» (TotalUchet) — метаприложение семьи: читает
summary.json приложений семьи и показывает неделю и месяц на одном экране.
Работать в корне C:ФPrognanandaClaude ProjsTotalUchet.

Прочитай CLAUDE.md и все файлы docs/ перед началом. Договор семьи —
src/shared/docs/02-Архитектура.md, «Договор семьи», «Срез итогов»;
решения ядра — src/shared/docs/05-Решения.md, особенно Я-15, Я-16,
Я-33, Я-34 (бот). Ядро подмодулем на cbf12b1, отсюда не правится;
в FamilyCore новее — сначала скажи, что изменилось и что из этого нам.

Состояние: Этапы 0–3 закрыты. Этап 4 — Телеграм-бот: развилки Р-23…Р-29,
код сделан (src/bot/, scripts/bot.mjs, «Настройки» → «Телеграм-бот»).
Бот завёл и проверил человек по 06-Процесс, «Телеграм-бот: как
завести» — итог скажу. Закрыть Этап 4 по Плану, «Готово когда»; что
не сошлось — развилкой с рекомендацией, решение Р-NN. Хвосты — План:
iPhone (Р-14), настоящие связки ещё не заведены.

Ключевое:
— показываем, а не досчитываем (Я-15): ни сумм, ни средних, ни цвета;
— все уместные блоки — сворачиваемыми через Fold ядра, с итогом
  у свёрнутого, на всех экранах; в Телеграме — blockquote expandable;
— список приложений, имена репозиториев, ключи групп и связки — записи
  человека, не код (Я-16, Я-22);
— репозиторий публичный (Р-01): тесты и прогон — на выдуманных срезах,
  подставном GitHub и Телеграме; ни токенов, ни настоящих срезов, ни
  названий групп — ни в коде, ни в тестах, ни в логах Actions, в том
  числе приватного репозитория бота (Я-33).

Как работаем: сначала план, код после подтверждения. Решения — в
docs/05-Решения.md в момент выбора. Отступления от Архитектуры и договора —
вслух до кода. Видимое изменение — строка в src/changes.ts тем же коммитом.
Перед пушем — npm test, npm run typecheck, npm run build, npm run smoke —
по коду выхода. Правки файлов с кириллицей — инструментом правки или
скриптом-файлом из папки сессии, не heredoc. В конце сессии — Журнал.
Перед /clear дерево чистое и всё запушено.
```
