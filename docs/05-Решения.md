# Решения

Какие были развилки, что выбрали и почему. Отвечает на вопрос «почему так, а не иначе».
Записывается в момент выбора, чтобы через месяц не переобсуждать заново.
Старые записи не переписываются: отмена — новая запись со ссылкой «отменяет Р-NN».

Свои решения — «Р-NN»; ядра — «Я-NN» (`src/shared/docs/05-Решения.md`); приложений — с хозяином: «Р-87 „Делу Время“».

---

### Р-01 · Репозиторий кода публичный; ни токенов, ни данных, ни примеров из настоящих срезов, ни содержимого срезов в логах
24.09.2026

Повод: вопрос человека в первой сессии — верно ли, что репозиторий публичный, или сделать приватным.

**Решено.** `goigolden88/TotalUchet` остаётся публичным, как вся семья. В нём никогда не бывает токенов, настоящих срезов и их кусков — **в том числе названий групп**: ключ `time.group.<ключ>` «Делу Время» — нормализованное название группы человека (Р-87 «Делу Время»). Тесты — на выдуманных срезах. Ни один workflow не печатает содержимое среза, токен или ответ API: логи Actions публичного репозитория видны всем.

Выбор человека, рекомендация.

**Почему.** GitHub Pages для приватного репозитория — только на платном тарифе. А приватность кода почти ничего не даёт: сайт на Pages открыт всем, собранный JS видит любой. Секретов в коде нет по определению (Я-16: токен — в `settings` устройства). Ядро `FamilyCore` публичное и подключается подмодулем без токена (Р-46, Р-50 «Трапезы») — у нас так же.

Отвергнуто: **приватный репозиторий** — платный Pages ради того, что и так видно на сайте.

**Цена.** Дисциплина вместо замка: выдуманные данные в тестах и документах, осторожность с логами. Телеграм-боту (Этап 4) это не подходит — ему нужен приватный репозиторий или логи без данных (План).

---

### Р-02 · Ядро — подмодулем; своей синхронизации нет; свои записи — с `Base`; один токен только на чтение
24.09.2026

Повод: стартовый промпт, развилка 1 — берёт ли метаприложение ядро (рекомендация сессии ядра — да) и нужна ли ему синхронизация.

**Решено.**

- **`FamilyCore` — подмодулем в `src/shared/`**, как у приложений семьи. Берём `parseSummary` и типы среза, даты, клиент GitHub, `createDb` с `settings` и файлом-копией, `Layout`, `Install`, «Что нового», `Report`, работник, каркас стилей, `familyVite`, иконки. `dbName` — `totaluchet`; в договор его вписывает сессия ядра
- **Своей синхронизации нет** — `createSync` не вызывается
- **Свои записи — список приложений и архив срезов — хранилищами с `Base`,** хотя синхронизации нет: файл-копия сливается по записи, а синхронизация, если понадобится, добавится без миграции. `places` выбраны сразу и замораживаются (Я-09)
- **Токен один:** fine-grained, «Contents: Read-only», на репозитории данных семьи (Я-16). Новое приложение — правкой списка репозиториев того же токена, не новым токеном. Токен в `settings` устройства, в файл-копию не входит

Выбор человека, рекомендация.

**Почему.** Всё нужное уже есть в ядре и проверено четырьмя приложениями; копия — ровно та беда, из которой ядро родилось (Р-45 «Трапезы»). Синхронизация своих записей требует права записи в свой репозиторий данных. **У fine-grained токена один набор прав на все выбранные репозитории:** токен с записью в свой репозиторий писал бы и в репозитории всех приложений — это нарушает «только на чтение» Я-16. Значит, синхронизация — это второй токен. Своих записей мало: список из пяти приложений переносится файлом.

Отвергнуто:
- **без ядра** — копия механики, форма среза без общей проверки;
- **синхронизация сразу** — второй токен ради пяти строк списка.

**Цена.**

1. Список приложений вводится на каждом устройстве или переносится файлом-копией.
2. Поля импорта `AppConfig` ядра обязательны, а импорта здесь нет — заглушки, вопрос сессии ядра.
3. Правка ядра под метаприложение — только сессией ядра и только если она осмысленна для любого приложения семьи.

---

### Р-03 · Имя — «Тотальный Учёт»; своё название — настройка устройства
24.09.2026

Повод: вопрос первой сессии — имя в интерфейсе. Человек: «Тотальный Учёт — но в настройках должна быть возможность смены названия».

**Решено.** Имя по умолчанию и в манифесте — «Тотальный Учёт». В «Настройках» его можно сменить. Своё название лежит в `settings` под ключом `title` и не синхронизируется. Шапка, заголовок вкладки и тексты, называющие приложение, берут имя из одного места — `src/ui/title.ts`. В тексте имя не склоняется: «приложение «Тотальный Учёт»». Решения снаружи называются «Р-NN „Тотального Учёта“».

Выбор человека. В ядре такой настройки нет — есть своя у «Делу Время» (Р-26 «Делу Время»: названия вкладок). Приём оттуда: одно место, тест-сторож на названии, написанном в исходнике руками.

**Почему.** Название — дело вкуса, а цена настройки при одном источнике — одна функция и тест.

Отвергнуто: **настройка в ядре для всей семьи** — может быть осмысленной для любого приложения, но это правка ядра; вопрос отдан сессии ядра, здесь не ждём.

**Цена.** Название у каждого устройства своё. Имя под иконкой зашито в манифест при установке и за настройкой не следует. Короткое имя под иконкой на Android может обрезаться — проверка на Этапе 0.

---

### Р-04 · Главный экран — «Сводка»: отрезок переключателем, по умолчанию идущая неделя; отрезок среза ищется по концам; строки — в порядке хозяина
24.09.2026

Повод: стартовый промпт, развилка 2 — что на первом экране, порядок, как видны свежесть, неокончательность и «не известно». Человек: «актуально, конечно, идущая, но и прошлая 100% где-то должна быть».

**Решено.**

- **Переключатель отрезка:** идущая неделя — по умолчанию, прошлая неделя, прошлый месяц, идущий месяц. Экран один на все четыре
- **Приложения** — в порядке, который задал человек в «Семье». **Строки** — в порядке, в котором их отдал хозяин; не сортируем: сортировка по числу — уже оценка
- **Отрезок среза ищется по концам, а не по месту в списке.** Отрезки среза считаются от его `computedOn`: срезы, посчитанные в разные дни, называют «прошлой неделей» разные недели. Нет нужной недели — «не известно: срез посчитан ДД.ММ, этой недели в нём нет»
- **Свежесть** — в шапке каждого приложения: «посчитано ДД.ММ · по записям по ДД.ММ». **Неокончательность** — «идёт, по ДД.ММ» у отрезка с `through`; при `null` — ничего (Я-21)
- **Основание** — под каждой строкой, всегда видно. **«Не известно»** — словами хозяина, приглушённо. **Вердикт** — словами, без цвета (Я-15)
- **Сопоставления групп соседей (Я-19) на первом экране нет** — пока срез отдаёт одно приложение, сопоставлять нечего. Этап 3

Выбор человека, рекомендация. Рекомендация по умолчанию была «прошлая неделя» — человек выбрал идущую.

**Почему.** Идущая неделя — то, про что вопрос «как дела» чаще всего; прошлая — одним тапом. Поиск по концам — единственное, что не врёт при срезах разной свежести. Порядок хозяина и основание всегда — продолжение Я-15 и правила семьи «число — вместе с основанием».

Отвергнуто:
- **отрезок по месту в списке** — неделя одного приложения рядом с другой неделей другого;
- **отдельные экраны «Неделя» и «Месяц»** — две копии одного показа;
- **основание по тапу** — число без основания на экране.

**Цена.** Идущая неделя в понедельник почти пуста — у многих строк «не известно» или ноль. Срез, посчитанный на прошлой неделе, на идущей неделе даёт только «не известно».

---

### Р-05 · Чтение среза: сначала доступ, потом файл; состояние словами у каждого приложения; без сети — последний увиденный
24.09.2026

Повод: развилки 1–2 — как читается `summary.json` и что видно, когда не вышло.

**Решено.** Порядок и тексты — `02-Архитектура`, «Чтение среза». Главное:

- **Доступ проверяется до файла.** Приватный репозиторий без доступа GitHub отдаёт тем же 404, что и несуществующий. Иначе «нет доступа» выглядело бы как «срез не отдаёт»
- **Нет `summary.json` — «срез не отдаёт»,** спокойным текстом (Я-16)
- **Ошибка одного приложения не трогает остальные**
- **Без сети и при кривом срезе** — последний увиденный со своей датой прочтения
- Читаем при открытии «Сводки» и кнопкой «Обновить»; в фоне — нет
- **Токен с правом записи** — предупреждение в «Настройках» (`canWrite` из `info()` ядра)

Рекомендация, человек согласился со всеми развилками.

**Почему.** Каждое состояние чтения — своё действие человека: нет токена — вписать; нет доступа — поправить токен или имя; срез не отдаёт — дело приложения. Одно «ошибка» их не различит.

**Цена.** На каждое приложение — запросы доступа, головы, дерева и, если срез новый, файла. Лимит токена — 5000 в час, в семье это не заметно.

---

### Р-06 · «Требует внимания» — блоком «Зовут» наверху «Сводки»; уведомления нет
24.09.2026

Повод: развилка 3 — «требует внимания» сначала экраном; уведомление вместо четырёх — позже, вместе с выключением тем у приложений (Я-18, открытый вопрос ядра).

**Решено.** Пункты `attention` последних срезов — наверху «Сводки», по приложениям: подпись, счёт, «за ДД.ММ», основание, «открыть» — адрес сайта приложения и `link`. Пусто — «приложения сейчас не зовут», не «всё сделано» (Я-21). Уведомлений метаприложение не шлёт, пока ядро не решит, как приложения перестают напоминать о темах из среза (Я-18).

Рекомендация, человек согласился.

**Почему.** Один экран на неделю: открыл — видишь и кто зовёт, и итоги. Отдельная вкладка ради списка из двух-трёх пунктов — лишний тап.

**Цена.** Пункт свеж настолько, насколько свеж срез: «зовут» с прошлого понедельника виден, пока приложение не пересчитает срез. Дата у пункта и «посчитано» у приложения — видны.

---

### Р-07 · «Семья» — ссылки и состояние среза; отметки «установлено» нет
24.09.2026

Повод: развилка 4 — ссылки на приложения и отметка «установлено» там, где браузер даёт узнать (`getInstalledRelatedApps`); установить соседа одной кнопкой браузер не позволяет.

**Решено.** Экран «Семья»: по приложению — имя, ссылка на сайт, состояние среза, подсказка «открой и нажми „Установить“» (экран установки есть у всех приложений из ядра). Здесь же правится список приложений. Отметки «установлено» нет. Открывает ли ссылка установленное приложение или вкладку браузера — проверка на телефоне, Этап 2.

Выбор человека, рекомендация.

**Почему.** `getInstalledRelatedApps` для соседа вне своего scope (developer.chrome.com, «Get Installed Related Apps API», проверено 24.09.2026):

- только Chrome на Android;
- нужен `/.well-known/assetlinks.json` в корне origin — отдельный репозиторий `goigolden88.github.io`;
- учитываются **только первые три** приложения из манифеста, а приложений будет пять;
- список соседей в манифесте — это код, а список приложений — настройка человека (Я-22); у друга к тому же другой origin.

Отвергнуто: **`assetlinks.json` через репозиторий `goigolden88.github.io`** — ещё один репозиторий ради отметки у трёх приложений из пяти и только на Android.

**Цена.** Что установлено, человек видит сам, на телефоне.

---

### Р-08 · Увиденные срезы — архивом на устройстве, по одному на приложение и день расчёта; экрана и синхронизации пока нет
24.09.2026

Повод: развилка 5 — история увиденных срезов: нужна ли и где (Я-17: в срезе её нет; «что видело, метаприложение запоминает само»). Человек: «с Б согласен, но может и В — хочется где-то видеть динамику для аналитики».

**Решено.**

- **Каждый новый срез ложится в хранилище `seen`** — по одной записи на приложение и день расчёта; позднее прочтение того же дня перезаписывает. Последний из них — ещё и то, что видно без сети (Р-05)
- **`id` — из приложения и `computedOn`, а не ULID.** Два устройства, прочитав один срез, дают одну запись — архив потом сливается без повторов
- **Экрана у архива пока нет**
- **Синхронизация архива** между устройствами — не сейчас, но без миграции: записи с `Base`, `places` — `seen/ГГГГ-ММ.json` — выбраны сразу (Р-02). Цена её — второй токен с записью в свой репозиторий данных
- **Экран динамики** — когда архив накопит месяцы. Это вопрос к Я-15, который принят предварительно: ряд значений по неделям — уже почти «серия». Решает сессия ядра вместе с пересмотром Я-15, до экрана

Выбор человека, рекомендация.

**Почему.** В срезе только две недели и два месяца. Если не копить сейчас, через полгода, когда дойдёт до динамики и аналитики связей (Я-10), взять прошлое будет неоткуда. Копить дёшево: около 10 МБ в год. Синхронизировать сейчас — второй токен ради архива, у которого ещё нет экрана.

Отвергнуто:
- **только последний срез** — прошлое теряется безвозвратно;
- **архив с синхронизацией сразу** — второй токен до первого читателя архива.

**Цена.**

1. Архив живёт в одном браузере; у установленного приложения на iPhone хранилище своё, отдельное от вкладки. Страховка — файл-копия.
2. Числа в архиве — «как видели тогда»: прошлое у приложений пересчитывается задним числом (Я-21), архив этого не знает.
3. Архив копится только тогда, когда приложение открывают: дни без открытия — дыры.

---

### Р-09 · `CoreProvider` получает свою заглушку синхронизации «выключено»; `createSync` не вызывается
24.09.2026

Повод: план Этапа 0. `Core` ядра (`src/shared/ui/core.tsx`) требует `sync: Sync`, а на `useCore()` держатся «Что нового» (`useWhatsNew`) и «Сообщить об ошибке» (`ReportBug`). Синхронизации у нас нет (Р-02, Я-23).

**Решено.** `noSync` в `src/app/core.ts` — восемь методов `Sync`, состояние всегда `off`, в сеть не ходит, настроек не пишет. `CoreProvider` получает её. Сессии ядра — пункт: `sync` в `Core` сделать необязательным; тогда заглушка уходит.

Рекомендация, человек согласился.

**Почему.** Заглушка держит Я-23 буквально: `createSync` не вызывается, ни одного пути к записи в коде нет. Отчёт об ошибке честно говорит «синхронизация: off».

Отвергнуто:
- **настоящий `createSync`, не включённый** — нарушает Я-23; путь к записи в код метаприложения появляется, пусть и выключенным;
- **свои «Что нового» и отчёт без `CoreProvider`** — копии ядра.

**Цена.** Меняется тип `Sync` в ядре — правим заглушку при обновлении указателя; ошибку покажет `tsc`.

---

### Р-10 · «Семья» на Этапе 1 — добавить, поправить и убрать; порядок — числом в той же форме
25.09.2026

Повод: план Этапа 1. План давал Этапу 1 только «добавить приложение», правку и удаление — Этапу 2.

**Решено.** На Этапе 1 «Семья» — список приложений и одна форма на добавление и правку: имя, репозиторий данных, сайт, порядок. Убрать — кнопкой у приложения; записи `seen` остаются (Архитектура, «Увиденный срез»). Перестановки отдельной нет: порядок — число в форме. На Этапе 2 остаются состояние среза у каждого приложения в «Семье», подсказка «Установить» и проверка ссылок на телефоне.

Рекомендация, человек согласился.

**Почему.** Без правки опечатку в имени репозитория не исправить, а проверка «неверное имя — „токен не видит репозиторий“» из «Готово когда» Этапа 1 оставила бы запись, которую не убрать.

Отвергнуто: **только «добавить», как в Плане** — ошибка ввода неисправима до Этапа 2.

**Цена.** Этап 1 чуть больше; порядок числом — неудобнее перетаскивания, но на пять строк хватает.

---

### Р-11 · Уточнения чтения: `canWrite` — ещё и строкой на «Сводке»; тот же `sha` — новый `readAt`
25.09.2026

Повод: план Этапа 1, чтение по Р-05.

**Решено.**

- **Предупреждение «токен шире, чем чтение»** — не только в «Настройках» (Р-05), но и одной строкой на «Сводке» со ссылкой в «Настройки». Чтение и так спрашивает `info()` и знает `canWrite`
- **Тот же `sha`, что у последнего увиденного,** — файл не качается, но у последнего увиденного обновляется `readAt`: это время последней проверки, что срез прежний. «Прочитан ДД.ММ ЧЧ:ММ» без сети показывает её, а не первое прочтение

Рекомендация, человек согласился.

**Почему.** Кнопку проверки доступа в «Настройках» можно не нажать ни разу, а токен с записью — главный риск Я-16. Без нового `readAt` срез без сети выглядел бы старее, чем он есть.

**Цена.** Одна запись в базу на приложение при каждом открытии «Сводки»; `updatedAt` записи `seen` двигается от проверки, а не от нового среза.

---

### Р-12 · Предупреждения «токен шире, чем чтение» нет: GitHub не сообщает права fine-grained токена. Отменяет предупреждение `canWrite` Р-05 и Р-11, п. 1
25.09.2026

Повод: проверка Этапа 1 на настоящем срезе. Человек выпустил токен с Contents: Read-only (скрин настроек токена), а «Сводка» показала «токен шире, чем чтение».

**Как было.** `canWrite` из `info()` ядра — это `permissions.push` ответа GitHub на репозиторий. Для fine-grained токена там, по всему видно, права аккаунта на репозиторий, а не права токена: у владельца — всегда `true`. Предупреждение горело бы у каждого, кто читает свои репозитории. На настоящем GitHub другое объяснение не проверялось; токен на скрине — Read-only.

**Решено.**

- Предупреждения нет ни на «Сводке», ни в «Настройках»; `canWrite` метаприложение не читает
- «Проверить доступ» показывает полное имя репозитория и строку: права токена GitHub не сообщает, Read-only выбирается при выпуске
- Сессии ядра — пункт: комментарий `RepoInfo.canWrite` («есть ли у токена право писать») для fine-grained токенов неверен; «Проверить доступ» у приложений, вероятно, говорит то же неверное
- Р-11, п. 2 — новый `readAt` при том же `sha` — в силе

Рекомендация, человек согласился.

**Почему.** Предупреждение, которое горит всегда, хуже, чем никакого: его перестают читать, и настоящая ошибка тонет.

Отвергнуто: **проверка пробной записью** (неверный запрос на запись: 403 — прав нет, 422 — есть) — у метаприложения не должно быть ни одного пути к записи, даже для проверки (Я-16, Р-09).

**Цена.** Токен с правом записи метаприложение не заметит: держит только выпуск по шагам (Contents: Read-only) — дисциплина человека.

---

### Р-13 · «Семья» на Этапе 2: чтение общее со «Сводкой»; приложения — свёрнутыми блоками; «Как установить» — один блок со ссылкой на каждое
25.09.2026

Повод: план Этапа 2 — состояние среза у каждого приложения в «Семье» и подсказка «Установить» (Р-07). Состояние чтения жило внутри «Сводки» и пропадало при переходе на другую вкладку.

**Решено.**

- **Состояние чтения — одно на оба экрана:** архив, проход чтения и «Обновить» — общим провайдером над маршрутами. Читается, как и было, при открытии экрана — теперь «Сводки» или «Семьи» (Р-05). Добавленное или поправленное в «Семье» приложение читается сразу, и «токен не видит репозиторий» видно под ним
- **Слова — прежние:** свежесть «посчитано ДД.ММ · по записям по ДД.ММ» и строка состояния чтения, как на «Сводке», а не новые «отдаёт», «нет доступа» из Архитектуры. Итог у свёрнутого — одной функцией на оба экрана: «посчитано ДД.ММ», «срез не отдаёт», «не прочитан»
- **Приложение в «Семье» — `Fold` ядра:** имя, у свёрнутого — итог; внутри состояние, репозиторий, ссылка на сайт, «Поправить», «Убрать». Нет токена — одна строка со ссылкой в «Настройки»
- **«Как установить» — один блок, свёрнут по умолчанию:** объяснение и список приложений со ссылкой на сайт каждого — там его собственная кнопка «Установить». Какое ставить, человек выбирает сам, по одному. Отметки «установлено» нет (Р-07)
- Ссылки — пока новой вкладкой, как на Этапе 1; как ведут себя на телефоне — проверка человека, итог — в Журнал и, если нужно, решением

Рекомендация, человек согласился; список ссылок внутри «Как установить» — по вопросу человека «нужен выбор, что установить».

**Почему.** Два прохода чтения — два итога и два места, где склеивается архив. Только архив в «Семье» — не видно «нет доступа», ради которого состояние туда и идёт. Подсказка у каждого из пяти приложений — пять одинаковых строк. Одной кнопкой «установить всё» браузер не позволяет: страница не ставит чужое приложение, каждое ставится со своего сайта (Р-07).

Отвергнуто:
- **«Семья» читает сама** — второй проход рядом с первым;
- **кнопка «Установить всё»** — браузер не даёт.

**Цена.** Переход между «Сводкой» и «Семьёй» — новый проход чтения, как было при каждом открытии «Сводки». Итог у свёрнутого блока «Сводки» без среза теперь «срез не отдаёт», а не пусто.

---

### Р-14 · Ссылки на приложения — как есть, новой вкладкой: открывается установленное приложение или вкладка браузера — оба годятся
25.09.2026

Повод: Этап 2, п. 2 — проверка человеком на телефоне, открывают ли ссылки «Семьи» и «открыть» из «Зовут» установленное приложение или вкладку браузера (Р-07, Р-13).

**Проверено.** Android: открывается то установленное приложение, то вкладка браузера; нужный экран и данные — есть. iPhone не проверялся.

**Решено.** Ссылки остаются как есть — `target="_blank"` на `site` и `site + '#' + link`. Выбрать, что откроется, страница не может: решает браузер и то, установлен ли сосед; у кого не установлен — откроется браузер, и это правильно. Ни `intent://`, ни подсказки «открывай с иконки» нет.

Выбор человека: «в целом оба варианта приемлемы».

**Почему.** Вопрос этапа — попадает ли человек в нужное приложение на нужном экране одним тапом; попадает. Открыть установленного соседа наверняка страница не может: `intent://` привязан к имени пакета WebAPK, своему на каждом устройстве (Р-07).

**Цена.** На iPhone не проверено. Там ссылка, вероятно, уходит в Safari, а у установленного соседа своё хранилище — откроется сосед без данных. Проверить, когда будет iPhone; не так — решением.

---

### Р-15 · Этап 3 ждёт среза «Дневников», а не любого второго; до него — сессия ядра, «Дневники», «Злотые»
25.09.2026

Повод: начало Этапа 3. Второй срез есть — «Трапеза» (Р-55 «Трапезы»), человек подтвердил: виден на «Сводке». План начинал Этап 3 со второго среза.

**Как было.** Я-19, п. 5 сопоставляет строки соседей «по группе». Срез «Трапезы» групп и категорий не отдаёт (Р-55 «Трапезы»: не просит никто) — только учёт недели, ккал и нормы. С «Делу Время» пара одна и слабая: группа времени про еду, если она у человека есть, рядом со строками учёта «Трапезы». Пара, ради которой этап, — тренировки «Дневников» рядом с группой «Делу Время» про движение (Журнал, 25.09.2026, «Промпты»).

**Решено.**

- **Этап 3 начинается, когда срез отдают «Дневники»** и он виден на «Сводке»
- **Порядок до него:** сессия ядра — сверить промпты приложениям с договором до их сессий, обновить состав; «Дневники»; «Злотые». Срез «Злотых» Этап 3 не держит — на ступени 1 Я-13 пар для «рядом» не даёт, — но «Сводке» нужен
- Пришёл срез «Злотых» с не календарным месяцем — правило сопоставления месяца решается здесь (открытый вопрос ниже), до или в начале Этапа 3

Выбор человека, рекомендация.

**Почему.** Механизм «рядом», построенный на одной слабой паре, подгоняется под неё: форма настройки, раскладка и переименование решаются на случае, которого человек, может быть, и не хочет видеть. Две настоящие пары проверяют решение сразу.

Отвергнуто: **Этап 3 сейчас на паре «Делу Время» — «Трапеза»** — решение без настоящего случая.

**Цена.** Этап 3 ждёт трёх чужих сессий. Здесь до того — ничего, кроме хвостов.

---

### Р-16 · Заглушки синхронизации нет: `CoreProvider` без `sync` (Я-29). Отменяет Р-09
25.09.2026

Повод: ответ ядра на Р-09 — Я-29, `sync` в `Core` необязателен; `useSyncStatus` без него — `off` (ядро `cb72d16`).

**Решено.** `<CoreProvider value={{ config, db }}>`; `noSync` из `src/app/core.ts` убран. Отчёт об ошибке говорит «Синхронизация: выключена» — как и с заглушкой; прогон проверяет эти слова. `SyncSettings` и «Забыть токен» ядра без `sync` бросают ошибку — у нас их нет на экранах, и ставить их — ошибка кода.

Рекомендация ядра, человек согласился.

**Почему.** Заглушка была выдумкой под обязательное поле; каждая правка типа `Sync` ломала бы её. Я-23 теперь держится буквально: ни `createSync`, ни кода, похожего на синхронизацию.

**Цена.** Нет.

---

### Р-17 · «Рядом» — связки: имя человека и строки из разных приложений, выбранные из увиденных срезов
25.09.2026

Повод: Этап 3, развилка 1 — как человек задаёт «рядом» (Я-19, п. 5). Настоящий случай — три строки тренировок «Дневников» (`training.count`, `training.minutes`, `training.km`) и одна строка группы «Делу Время» (`time.group.<ключ>`): это набор, а не пара.

**Решено.**

- **Связка** — запись человека: его имя связки и строки, каждая — приложение (`id` записи `App`) и `key`. Строк одного приложения — сколько угодно
- **Строки выбираются из увиденных**, а не вписываются ключом: список строк последних увиденных срезов — имя приложения и подпись. Ключ сохраняется, подпись — нет: её отдаёт хозяин
- **Строка — не больше чем в одной связке:** на экране она не повторяется в двух местах
- **У связки нет своего числа** — ни суммы, ни разности, ни доли (Я-15): это раскладка
- Имя связки, приложения и ключи — данные устройства, не код (Я-16, Я-22, Р-01)

Выбор человека, рекомендация.

**Почему.** Одна запись закрывает настоящий случай целиком; выбор из увиденного исключает опечатку в ключе; связка ничего не считает.

Отвергнуто:
- **пары строк** — набор из трёх и больше — склейка пар, порядок показа не ясен;
- **правило по семействам ключей** (`training.*`) — ключи «Дневников» постоянны, у группы «Делу Время» ключ один: шаблон ничего не даёт сверх выбора, зато требует синтаксиса;
- **подбор по похожим подписям** — «рядом» задаёт человек (Я-19, п. 5); угадывание — уже оценка.

**Цена.** Строку, которой не было ни в одном увиденном срезе, не выбрать — например, группу без времени. Имя связки может совпасть с названием группы — это данные устройства, в репозиторий оно не попадает.

---

### Р-18 · Связки — хранилище `bundles` с `Base`; схема 2, первая миграция — добавляющая
25.09.2026

Повод: Этап 3, развилка 2 — где лежат связки (Р-17).

**Решено.**

```ts
type Bundle = Base & {
  name: string                           // имя связки — вписывает человек
  order: number                          // порядок на «Сводке», как у App
  rows: { app: string; key: string }[]   // app — id записи App
}
```

- **Схема 2:** шаг `additive: true` заводит хранилище `bundles` с индексом `updatedAt`. `v1Stores` не трогается — новое хранилище заводит только миграция (Я-08); файл-копия версии 1 принимается
- **Место** — `bundles.json` одним файлом, как `apps.json`; замораживается с релизом (Я-09)
- **Едут файлом-копией вместе с `apps`:** строки связки ссылаются на `id` приложений, а они переезжают только той же копией

Выбор человека, рекомендация.

**Почему.** Связки — записи человека того же рода, что список приложений, и живут так же: хранилище с `Base`, место, файл-копия; синхронизация, если появится, добавит их без миграции (Р-02, Р-08).

Отвергнуто:
- **`settings` устройства** — не входят в файл-копию: связки не переезжают и пропадают с базой;
- **внутри записи `App`** — одна связка размазана по записям: переименование правит все, «строка не больше чем в одной связке» проверяется по всем.

**Цена.** Схема 2; старая сборка на файле-копии версии 2 скажет «обнови приложение» — механика ядра. Имя хранилища и `bundles.json` — навсегда.

---

### Р-19 · Связка на «Сводке» — свёрнутый блок между переключателем и приложениями; внутри — по приложению со своей свежестью, столбиком; блоки приложений не меняются
25.09.2026

Повод: Этап 3, развилка 3 — как видна связка (Р-17, Р-18).

**Решено.**

- **Место** — между переключателем отрезка и блоками приложений: связка показывает выбранный отрезок. Каждая — свой `Fold` с её именем, по умолчанию развёрнута; что свёрнуто, помнит устройство
- **Внутри** — по приложениям в порядке человека. У каждого — своя свежесть: «посчитано · по записям по», «идёт, по» или «не известно: этой недели в срезе нет» — теми же функциями, что в блоке приложения. Строки — в порядке хозяина, с основанием
- **Столбиком по приложениям, не таблицей**
- **Итог у свёрнутой** — имена приложений через «·», без чисел, как у «Зовут»
- **Блоки приложений не меняются:** строка из связки остаётся и у своего приложения

Выбор человека, рекомендация.

**Почему.** Срезы посчитаны в разные дни — две строки рядом без своей свежести врут. Блок приложения — срез как отдал хозяин (Р-04); связка — второй взгляд на те же строки, а не их новое место.

Отвергнуто:
- **убирать строку из блока приложения** — блок перестаёт быть срезом хозяина, порядок хозяина рвётся;
- **таблица приложение × строка** — колонки чисел просят сравнения (Я-15), на телефоне не помещаются;
- **числа в итоге свёрнутой** — длинно и по форме почти сводная.

**Цена.** Одно число на экране дважды — в связке и в блоке приложения; блоки приложений сворачиваются.

---

### Р-20 · Строки связки нет — строка словами, без догадок; ключ сам не переназначается; строки убранного приложения остаются в записи
25.09.2026

Повод: Этап 3, развилка 4 — что видно, когда строки связки нет в срезе (Я-17, «Цена», п. 3: переименование группы «Делу Время» меняет ключ).

**Решено.**

- **Нет в этом отрезке, есть в других отрезках среза** — приглушённо «<подпись> — нет в срезе за этот отрезок». Обычное дело: нормы «Делу Время» — только недели, фон — только больше нуля, недели «Злотых» — `not-provided`
- **Нет ни в одном отрезке последнего увиденного среза** — «<подпись> — в срезе от ДД.ММ этой строки нет ни в одном отрезке — поправь связку». Без «наверное, переименовали»: отсутствие строки не толкуется (Я-21). В форме правки такая строка помечена
- **Подпись пропавшей строки** — из архива `seen`: последний увиденный срез этого приложения, где строка была. Нет и там — ключ
- **Ключ сам не переназначается,** даже на новый с той же подписью: «старая группа — это новая» решает человек
- **Приложение убрано из «Семьи»** — на «Сводке» его строк в связке нет; в форме правки — «приложение убрано», убираются кнопкой. Из записи сами не удаляются: вернулось файлом-копией с тем же `id` — связка ожила
- **Приложение не прочитано или среза не отдаёт** — в связке его `stateLine`, как в блоке

Выбор человека, рекомендация.

**Почему.** Молча пропавшая строка — связка тихо худеет, и повода поправить не видно. Переназначение по подписи — угадывание (Р-17).

**Цена.** Понять, что пора поправить, можно только по строке «нет ни в одном отрезке». Поправить может только человек.

---

### Р-21 · Связки правятся в «Семье»; «Сводка» ведёт туда ссылкой
25.09.2026

Повод: Этап 3, развилка 5 — где заводятся и правятся связки.

**Решено.**

- **«Семья» — блок «Связки»** ниже приложений, выше «Как установить». Связка — `Fold`: свёрнутая — имена приложений; внутри — строки, «Поправить» (форма в том же блоке), «Убрать». Внизу — «Новая связка»
- **Форма:** имя, порядок числом (как у приложений, Р-10), строки — по приложениям в порядке человека, флажок и подпись, в порядке хозяина по последнему увиденному срезу (все четыре отрезка вместе). Строка из другой связки — недоступна, «в связке „X“» (Р-17). Пропавшие (Р-20) и строки убранного приложения — с пометкой, флажок снимается. Сохранить — есть имя и хотя бы одна строка
- **«Сводка»** — в связке ссылка «поправить» на неё в «Семье»; туда же — «поправь связку» из строки Р-20. Связок нет — «Сводка» о них молчит; новое — строкой «Что нового»

Выбор человека, рекомендация.

**Почему.** Записи человека правятся в одном месте, рядом со списком приложений; «Сводка» остаётся показом (Р-04).

Отвергнуто:
- **форма на «Сводке»** — главный экран обрастает формами;
- **«Настройки»** — там настройки устройства, а связки — записи человека (Р-18).

**Цена.** Завести связку — уйти со «Сводки»; строки в форме — подписями, без чисел.

---

### Р-22 · Строки связки — любые: ни запрета, ни пометки по смыслу ключа
25.09.2026

Повод: Этап 3, развилка 6; ответ ядра (Журнал `FamilyCore`, 25.09.2026, «Не описано»): `kcal.perDay` «Трапезы» — среднее хозяина, «Делу Время» средних не отдаёт — ставить ли их рядом; `norm.*` — у двух приложений.

**Решено.** Что класть в связку, решает человек (Р-17). Ни запрета, ни пометки «среднее», ни особой обработки `norm.*`: смысл строки говорит основание хозяина, оно всегда видно (Р-04). `norm.*` разных приложений — разные строки, различаются приложением.

Выбор человека, рекомендация.

**Почему.** Запрет или пометка по ключу — знание модели чужого приложения в коде метаприложения (Я-17: форма показателя, а не перечень); у «Дачи» тот же ключ может значить другое. Между строками ничего не считается (Я-15) — среднее с суммой нигде не складывается и не сравнивается машинно.

Отвергнуто:
- **запрет в форме** — список ключей чужих моделей в коде;
- **пометка «среднее»** — оценка сверх основания хозяина.

**Цена.** Среднее рядом с суммой экран не предупредит — только основание под строкой.

---

### Р-23 · Телеграм-бот: код — здесь, в `src/bot/`; запуск — плановым workflow приватного репозитория, который берёт `TotalUchet` с `main`
25.09.2026

Повод: Этап 4, развилка 1 — где живёт бот. Рамка ядра — Я-33, Я-34: бот — названный потребитель в составе «Сводки», только личный чат, свой токен чтения в секрете Actions; где живёт — решение «Тотального Учёта».

**Решено.**

- **Код бота — здесь, `src/bot/`,** публично: сообщение из срезов — функциями `view/`, отправка в Телеграм, склейка. Тесты — на выдуманных срезах, подставном GitHub и подставном Телеграме (Р-01). Ни одного настоящего имени
- **Запуск — приватный репозиторий человека** (имя — его, в код не попадает). В нём только workflow по расписанию, секреты и список приложений: имена репозиториев данных — записи человека (Я-16, Я-22). Workflow берёт `TotalUchet` **с `main`**, с подмодулем ядра, — оба публичны, токен не нужен. Образец workflow — в документах здесь; завести репозиторий и секреты — руками человека
- **В логах — ничего из среза, токенов и ответов API** и в приватном репозитории (Я-33)

Выбор человека, рекомендация.

**Почему.** Логи Actions публичного репозитория видны всем: ошибка, напечатавшая сообщение, раскрыла бы дни болезни — «логи без данных» здесь держится дисциплиной, а не замком (Р-01, «Цена»). Плановый workflow публичного репозитория выключается после 60 дней без активности — бот замолчал бы тихо. Код здесь — одна функция показа на «Сводку» и бота (Я-33), тесты рядом. `main` вместо указателя подмодулем — на один забываемый указатель меньше.

Отвергнуто:
- **всё здесь** — логи публичны, 60 дней, список приложений — JSON в секрете;
- **весь код бота в приватном репозитории** — функции показа и тесты раздваиваются;
- **`TotalUchet` подмодулем в приватном репозитории** — указатель после каждой правки бота.

**Цена.** Сломанный `main` роняет следующий запуск бота — страховка: тесты перед пушем, письмо GitHub об упавшем workflow. 2000 бесплатных минут приватного репозитория в месяц — запуск раз в неделю занимает около минуты.

---

### Р-24 · Бот пишет по понедельникам в 09:00 — прошлая неделя; 1-го числа в 09:00 — прошлый месяц, отдельным сообщением; ручной запуск с выбором отрезка
25.09.2026

Повод: Этап 4, развилка 2 — расписание и какие отрезки.

**Решено.**

- **Понедельник, 09:00 по времени человека (+05:00)** — прошлая неделя; **1-го числа, 09:00** — прошлый месяц, отдельным сообщением. Выпало 1-е на понедельник — два сообщения. В расписании Actions — UTC: `0 4 * * 1` и `0 4 1 * *`
- **Отрезок экрана — от дня запуска** (`screenPeriod`), отрезок среза — по концам (Р-04): срез, посчитанный в воскресенье, называет эту неделю идущей, и она находится
- **Ручной запуск** из Actions с выбором отрезка — для проверки без ожидания понедельника

Выбор человека, рекомендация.

**Почему.** В понедельник утром неделя закрыта; «идёт, по ДД.ММ» у среза, посчитанного раньше воскресенья, видно, как на «Сводке». Неделя и месяц одним сообщением — двойная длина раз в месяц.

Отвергнуто: **воскресенье вечером, идущая неделя** — неделя не закончилась, срезы обычно посчитаны днём раньше.

**Цена.** Расписание Actions опаздывает на минуты, бывает до часа. Бот не освежает срезы: уходит то, что приложение положило, когда его открывали с сетью (Я-11).

---

### Р-25 · Секреты бота — `READ_TOKEN`, `TELEGRAM_TOKEN`, `TELEGRAM_CHAT`; «только личный чат» проверяет код
25.09.2026

Повод: Этап 4, развилка 3 — секреты и как держится «только личный чат» (Я-33, Я-34).

**Решено.**

- **Секреты приватного репозитория** (Р-23): `READ_TOKEN` — свой токен бота, Contents: Read-only на репозитории данных семьи, выпуск и срок — в один день с токеном чтения «Тотального Учёта» (Я-34); `TELEGRAM_TOKEN` — от @BotFather; `TELEGRAM_CHAT` — id личного чата человека с ботом: `/start` боту, затем `getUpdates` Bot API в браузере, один раз
- **Замок на Я-33:** перед отправкой — `getChat`; тип не `private` — ничего не отправляется, workflow падает словами «чат не личный — бот пишет только в личный (Я-33)»
- **Второй замок — в @BotFather:** `/setjoingroups` → Disable
- **В инструкцию бота строкой:** превью уведомлений на экране блокировки скрывается настройкой Телеграма (ответ ядра, Журнал `FamilyCore`, 25.09.2026)

Выбор человека, рекомендация.

**Почему.** Правило «только личный чат», которое держит только дисциплина, ломается одной опечаткой в id; проверка типа чата — одна строка кода.

**Цена.** Утёк токен бота — чужой пишет от имени бота и читает то, что человек пишет боту; историю чата бот не видит. Отзыв — `/revoke` у @BotFather.

---

### Р-26 · Сообщение бота — то же, что «Сводка», теми же функциями `view/`; одно со звуком, приложение — сворачиваемой цитатой; не влезло — продолжение без звука целыми приложениями
25.09.2026

Повод: Этап 4, развилка 4 — что и в каком виде уходит в Телеграм (Я-33: состав «Сводки»).

**Решено.**

- **Состав — «Сводка»,** функциями `view/`, своих слов о данных нет, ничего не считается (Я-15):
  1. шапка — отрезок словами и датами;
  2. «Зовут» — подпись, счёт, «за ДД.ММ», основание, «открыть» — `site + '#' + link`, с id записи, если его отдал хозяин (Я-31, Я-33); пусто — «приложения сейчас не зовут»;
  3. по приложению в порядке человека: имя, свежесть, «идёт, по» или «не известно…» — всегда видны; строки «подпись — значение» и основание под каждой курсивом — внутри `blockquote expandable`, аналога `Fold`: свёрнутая показывает первые строки;
  4. внизу — ссылка на «Сводку»
- **Одно сообщение со звуком.** Больше 4096 знаков — продолжение без звука (`disable_notification`), приложения не рвутся; одно приложение больше лимита — по строкам
- **Разметка — HTML Bot API;** подписи и основания экранируются

Выбор человека, рекомендация.

**Почему.** Четыре уведомления вместо одного — то, от чего метаприложение уводит (`01`, «Проблема»). Число без основания — против Р-04, а с основаниями одно «Делу Время» может занять половину лимита — отсюда свёртка и перенос.

Отвергнуто:
- **сообщение на приложение** — чат пухнет;
- **без оснований** — Р-04;
- **основания списком в конце** — число и основание врозь.

**Цена.** Числа — после тапа по цитате. Как ссылки открываются из Телеграма — встроенный браузер или установленное приложение — проверка человека на телефоне.

---

### Р-27 · Связок в сообщении бота пока нет
25.09.2026

Повод: Этап 4, развилка 5 — связки в сообщении или нет (Р-17…Р-22).

**Решено.** Сообщение бота — «Зовут» и блоки приложений (Р-26), без связок. Вернуться, когда настоящие связки заведены и сверены на «Сводке» (План, «Хвосты», Этап 3); `bundleView` уже есть, формат списка для бота (Р-28) оставляет им место.

Выбор человека, рекомендация.

**Почему.** Показ связки на настоящих срезах не сверен. У бота нет архива увиденных срезов — у пропавшей строки вместо подписи из архива (Р-20) был бы ключ, у группы «Делу Время» — нормализованное название группы. Связка повторяет числа блоков (Р-19), а сообщение и так у лимита.

Отвергнуто: **связки сразу** — непроверенный показ и ключи вместо подписей.

**Цена.** Подборки строк человека в Телеграм пока не приходят.

---

### Р-28 · Список для бота — файл `bot.json` из «Настроек»: приложения, своё название, адрес «Сводки»; человек кладёт его в приватный репозиторий
25.09.2026

Повод: Этап 4, развилка 6 — у бота нет IndexedDB, а список приложений — запись человека на устройстве (Я-16, Я-22).

**Решено.**

- **«Настройки» — блок «Телеграм-бот»** (`Fold`): объяснение и «Скачать список для бота» — файл `bot.json`: записи `apps` в форме Архитектуры, своё название (`settings.title`, Р-03) — им бот подписывает сообщение, адрес «Сводки» — из адреса страницы в момент скачивания. Ни архива, ни токена, ни связок
- **Человек кладёт файл в приватный репозиторий** (Р-23) — веб GitHub, «Add file → Upload». Список поменялся — скачать и положить снова
- **Бот проверяет файл той же проверкой формы, что «Семья»** (`app/apps.ts`)

Выбор человека, рекомендация.

**Почему.** Список правится в одном месте — «Семья»; ни одного имени руками второй раз. Ни одного адреса в коде — ни соседей, ни своего.

Отвергнуто:
- **JSON руками в приватном репозитории** — дубль записей, опечатка всплывает в понедельник;
- **файл-копия целиком** — архив настоящих срезов едет в ещё один репозиторий без нужды.

**Цена.** Не положил новый файл — бот не знает нового приложения и пишет об убранном. Видимое изменение — строка «Что нового».

---

### Р-29 · Бот говорит об ошибках в Телеграм; упавший workflow — только когда Телеграм недоступен; об истечении токена — за 30 дней
25.09.2026

Повод: Этап 4, развилка 7 — как бот говорит об ошибке чтения и об истёкшем токене (Я-34, «Цена»).

**Решено.**

- **Ошибка чтения одного приложения** — в его блоке строка состояния словами «Сводки» (`stateLine`); остальные — как обычно. Архива у бота нет — «последнего увиденного» нет
- **401 у всех приложений** — одна строка наверху вместо строки у каждого: токен бота не принят — истёк или отозван, выпустить новый и вписать в `READ_TOKEN` (Я-34)
- **Токен истекает через 30 дней или раньше** — строка в шапке: выпустить новый вместе с токеном чтения «Тотального Учёта». Дата — заголовок GitHub, `tokenExpiry()` клиента ядра. Порог — своя константа с тем же числом, что `WARN_DAYS` ядра: тот лежит в `ui/SyncSettings.tsx`, а Node `.tsx` не запускает
- **`bot.json` не читается** — сообщение в Телеграм: список приложений не читается, почему
- **Телеграм недоступен** — неверный токен бота, чат не личный (Р-25), нет сети: workflow падает, GitHub шлёт письмо. В лог — только свои слова и код ответа, без тела: Телеграм повторяет в ответе текст сообщения (Я-33)

Выбор человека, рекомендация.

**Почему.** Человек смотрит в Телеграм, а не в Actions; письмо GitHub — для случая, когда сказать больше негде. Даты двух токенов чтения совпадают (Я-34) — предупреждение бота напоминает и о токене метаприложения.

**Цена.** Если GitHub не запустит workflow вовсе — сбой Actions, — бот промолчит, и сказать об этом некому.

---

## Открытые вопросы

- **Месяц, который не календарный** (Я-12, `periodStartDay` «Злотых») — как его сопоставлять с календарным. Когда появится первый такой срез. Пока спит: «Злотые» отдают календарный месяц, `periodStartDay` их расчёты не читают (Р-50 «Злотых»).
- **Экран динамики по архиву** — вместе с пересмотром Я-15 сессией ядра (Р-08).
- **Уведомление вместо четырёх** — после решения ядра по Я-18.
- **Телеграм-бот** — где живёт, что уходит в Телеграм. Этап 4, правка состава — сессией ядра.
